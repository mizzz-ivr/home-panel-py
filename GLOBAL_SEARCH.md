# 横断検索仕様

## 目的

Home Panelに保存したToDo・日別メモ・時間記録・習慣を、個別画面を順番に確認せず1画面から探せるようにします。

初期実装は既存SQLiteを読み取るだけとし、DBスキーマ、全文検索エンジン、保存データは変更しません。

## URL

```text
GET /search
GET /search?q=<検索語>
```

`q`未指定時は検索入力画面を表示します。

## 検索対象

| カテゴリ | 対象項目 | 結果からの遷移 |
|---|---|---|
| ToDo | タイトル | ダッシュボードのToDoカード |
| 日別メモ | 本文 | 該当日の履歴 |
| 時間記録 | カテゴリ・メモ | 該当日の履歴 |
| 習慣 | 習慣名 | 習慣管理画面の該当項目 |

習慣は利用中・終了済みの両方を検索します。

## 入力仕様

- 前後の空白を除去してから検証
- 2〜100文字
- 1文字以下または101文字以上は400
- `q`自体がない場合はエラーにせず初期画面を表示
- 空文字や空白だけを明示送信した場合は400

2文字未満を拒否することで、空検索や極端に広い検索による不要な全件走査を避けます。

## 検索方式

SQLiteとSQLAlchemyの`ILIKE`を使用した部分一致検索です。

```text
%<検索語>%
```

検索語に含まれる次の文字は、LIKEのワイルドカードではなく通常文字として扱います。

```text
\  %  _
```

エスケープ順序:

1. `\`を`\\`へ変換
2. `%`を`\%`へ変換
3. `_`を`\_`へ変換

ASCII文字については大文字・小文字を区別しません。日本語には大文字・小文字の区別がありません。

## 件数と並び順

カテゴリごとに最大20件、合計最大80件です。

| カテゴリ | 並び順 |
|---|---|
| ToDo | 更新日時の新しい順、同日時はIDの大きい順 |
| 日別メモ | メモ日付の新しい順、同日はIDの大きい順 |
| 時間記録 | 記録日の新しい順、作成日時、IDの順 |
| 習慣 | 更新日時の新しい順、同日時はIDの大きい順 |

初期版では総件数取得用の追加COUNTクエリを行いません。画面に表示する件数は取得できた結果数です。

## 表示内容

検索サービスはORMオブジェクトをテンプレートへ直接渡さず、表示専用の不変データへ変換します。

共通項目:

- ID
- タイトル
- 説明
- メタ情報
- 遷移先URL

メモ本文・時間記録メモは次の処理を行います。

- 改行・連続空白を単一空白へ正規化
- 120文字以内
- 121文字以上は末尾を`…`として省略

保存値と検索語はJinja2の自動エスケープで描画します。HTMLとして解釈する`safe`処理や検索語ハイライトは使用しません。

## HTTPヘッダー

検索画面には次を設定します。

```text
Cache-Control: no-store
X-Content-Type-Options: nosniff
```

検索語と検索結果をブラウザキャッシュへ積極的に残さず、MIMEスニッフィングを抑止します。

## データ整合性

横断検索は読み取り専用です。

- INSERTなし
- UPDATEなし
- DELETEなし
- コミットなし
- `app_settings`を含む設定更新なし

テストでは検索前後の主要4テーブルの件数が変わらないことを確認します。

## テスト観点

- ToDo・日別メモ・時間記録・習慣の横断検索
- 各結果の遷移先
- `q`未指定時の初期画面
- 2文字・100文字の正常境界
- 1文字以下・101文字以上の拒否
- `%`・`_`をリテラルとして扱うこと
- ASCIIの大文字・小文字を区別しないこと
- カテゴリごとの20件上限
- 新しい順の並び
- 保存データと検索語のHTMLエスケープ
- 検索前後でDB件数が変わらないこと
- ダッシュボードの検索フォーム
- ToDo・習慣の遷移アンカー
- 既存機能の回帰

## 影響範囲

### 変更対象

- 横断検索サービス
- 横断検索ルート
- 横断検索画面・CSS
- ダッシュボードの検索フォーム
- ToDoカード・習慣管理項目の遷移アンカー
- 横断検索テスト

### 変更しない範囲

- DBスキーマ
- 既存データ
- ToDo・メモ・時間記録・習慣の更新処理
- 集計処理
- CSV
- JSONバックアップ・検証・復元・世代管理
- ダッシュボード設定
- Pending Undo

## 制約・今後の拡張候補

- SQLite FTSなどの全文検索インデックスは未対応
- 複数語のAND・OR検索は未対応
- ページネーションは未対応
- 検索語ハイライトは未対応
- 日付・時間数・達成日による検索は未対応
- 検索結果のカテゴリ絞り込みは未対応
- ToDoカードを非表示にしている場合、ダッシュボードへ戻ってもToDoアンカーは表示されない
- 認証・ユーザー分離は未対応
- 実ブラウザ・スマートフォン・Safariでの確認は未実施
