# Home Panel 習慣の対象曜日設定

## 目的

習慣を毎日だけでなく、平日・土日・任意の曜日で管理します。

対象例:

- 平日だけ学習する
- 月・水・金に運動する
- 土日に振り返りを行う

対象外曜日は未達成として扱わず、達成率の分母へ含めません。

## 設定方法

`/habits/manage`の対象曜日欄で、月曜日〜日曜日を1つ以上選択します。

表示例:

- 全曜日: `毎日`
- 月曜日〜金曜日: `平日`
- 土曜日・日曜日: `土日`
- 月曜日・水曜日・金曜日: `月・水・金`

曜日を1つも選択しない設定は保存できません。

## 適用日

曜日設定は保存した日から適用します。

```text
7月1日〜7月9日: 毎日
7月10日〜: 月・水・金
```

7月9日以前のレポートは毎日設定のままです。7月10日以降だけ月・水・金として計算します。

同じ日に複数回変更した場合は、その日の設定期間を更新し、重複する履歴を作りません。

## ダッシュボード

アクティブな習慣は、今日が対象外でもダッシュボードに表示します。

対象外の場合:

- 対象曜日を表示
- `今日は対象外`と表示
- 達成ボタンを無効化
- 今日の達成件数の分母へ含めない

直接HTTPリクエストを送っても、対象外曜日の新しい達成記録は保存しません。

既に不整合な達成記録が存在する場合は、取り消し操作だけを許可します。

## 達成率

習慣の達成対象日は、以下の両方を満たす日です。

1. `habit_active_periods`の有効期間内
2. `habit_schedule_periods`の曜日設定で対象曜日

```text
達成率 = 対象日に達成した件数 ÷ 対象日の総数 × 100
```

月・水・金の習慣では、火・木・土・日は分子にも分母にも含めません。

## 連続日数

表示上は既存互換のため「日継続」と表記しますが、曜日指定習慣では対象日の連続達成回数です。

例:

```text
対象曜日: 月・水・金
月曜: 達成
水曜: 達成
金曜: 達成
```

結果は`3日継続`です。火曜・木曜は対象外のため連続を切りません。

今日が対象日で未達成の場合は、今日の途中であることを考慮し、直前の対象日までの連続を表示します。

## 曜日変更時の達成記録

変更日に既に達成記録があり、その曜日を対象外にしようとした場合は保存を拒否します。

先に当日の達成記録を取り消してから曜日を変更します。

過去日の設定変更UIは提供しません。これにより、過去レポートを意図せず書き換える操作を防ぎます。

## 終了済み習慣

終了済み習慣も曜日設定を変更できます。

- 達成履歴を保持
- 曜日設定履歴を保持
- 終了中に設定した曜日は再開後に利用
- 再開日以前のレポートは過去設定のまま

習慣の有効期間と曜日設定期間は別テーブルで管理します。

## データ設計

### `habit_schedule_periods`

| 項目 | 内容 |
|---|---|
| `id` | 曜日設定期間ID |
| `habit_id` | 対象習慣ID |
| `schedule_type` | 現在は`weekdays`固定 |
| `weekdays_mask` | 月曜0〜日曜6のビットマスク |
| `started_on` | 設定の適用開始日 |
| `ended_on` | 適用終了日。現在設定はnull |
| `created_at` | 登録日時 |

DB内部では曜日を7ビットで保存します。

```text
月=1
火=2
水=4
木=8
金=16
土=32
日=64
```

例:

```text
月・水・金 = 1 + 4 + 16 = 21
平日 = 31
毎日 = 127
```

制約:

- `schedule_type = weekdays`
- `weekdays_mask`は1〜127
- 終了日は開始日以降
- 同じ習慣・開始日は1件だけ
- 習慣削除時は関連期間も削除

## 既存SQLiteの移行

既存習慣には、作成日から始まる毎日設定を1件生成します。

```text
schedule_type = weekdays
weekdays_mask = 127
started_on = 習慣作成日
ended_on = null
```

これにより、移行前の集計結果は変わりません。

移行処理は冪等で、複数回実行しても設定期間を重複作成しません。

## バックアップ

JSONバックアップv5では`habit_schedule_periods`を保存します。

JSONは可読性を優先し、内部ビットマスクではなく曜日配列を利用します。

```json
{
  "id": 1,
  "habit_id": 1,
  "schedule_type": "weekdays",
  "weekdays": [0, 2, 4],
  "started_on": "2026-07-10",
  "ended_on": null,
  "created_at": "2026-07-10T09:00:00Z"
}
```

詳細は`BACKUP.md`を参照してください。

## 未対応

- 曜日を固定しない週N回
- 隔週
- 毎月特定日
- 祝日除外
- 過去日の曜日設定編集
- タイムゾーン設定
- 通知・リマインダー
