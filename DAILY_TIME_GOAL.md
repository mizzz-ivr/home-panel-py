# 1日の時間目標仕様

## 目的

当日の時間記録に対して「今日は何分取り組むか」を設定し、ダッシュボードを開いた時点で進捗・残り時間・達成状況を確認できるようにします。

時間目標は実績そのものではなく、ダッシュボード表示のためのユーザー設定として扱います。

## 保存先

設定キー:

```text
daily_time_goal_minutes
```

保存テーブル:

```text
app_settings
```

保存値はJSON整数です。

例:

```text
120
```

専用テーブルやDBマイグレーションは追加しません。

## 設定範囲

ユーザーが設定できる値:

```text
1〜1440分
```

- 整数のみ
- 1分を最小値とする
- 1440分を1日の上限とする
- 小数、文字列、負数、1441以上は400で拒否
- 不正入力時は既存設定を変更しない

設定解除操作では内部的に`0`をPOSTします。`0`は保存せず、設定キー自体を削除します。

## エンドポイント

### 設定・変更

```text
POST /settings/daily-time-goal
```

フォーム:

```text
minutes=120
```

成功時:

```text
303 /#time-card
```

### 設定解除

同じエンドポイントへ次を送ります。

```text
minutes=0
```

`daily_time_goal_minutes`だけを削除します。時間記録は削除・変更しません。

## 進捗計算

当日合計を`total_minutes`、目標を`goal_minutes`とします。

達成率:

```text
total_minutes * 100 // goal_minutes
```

例:

```text
95 / 120分 = 79%
120 / 120分 = 100%
150 / 120分 = 125%
```

実際の達成率は100%を超えて表示します。

プログレスバーだけは表示上の上限を100%にします。

```text
progress_percentage = min(percentage, 100)
```

### 未達成

```text
remaining_minutes = goal_minutes - total_minutes
```

例:

```text
目標120分 / 実績95分
→ 79%
→ 残り25分
```

### 達成

実績が目標以上なら達成です。

超過時間:

```text
exceeded_minutes = total_minutes - goal_minutes
```

例:

```text
目標120分 / 実績150分
→ 125%
→ 目標達成
→ 30分超過
```

## 時間記録カード

`app/templates/cards/time.html`に次を表示します。

- 当日合計
- 1日の目標
- 達成率
- プログレスバー
- 未達成時の残り時間
- 達成時の達成メッセージ
- 超過時の超過時間
- 目標設定・変更フォーム
- 目標解除ボタン

目標未設定時は、従来の時間記録機能を維持したまま設定フォームだけを表示します。

## Today Focus

Today Focusの「今日の記録時間」へ目標進捗を追加します。

目標未設定:

```text
1時間35分
1件の記録
```

目標設定済み・未達成:

```text
1時間35分
79%・残り25分
```

目標達成済み:

```text
2時間30分
125%・目標達成
```

Today Focus自体は引き続き読み取り専用です。目標変更は時間記録カードで行います。

リンク先:

```text
/?show_card=time#time-card
```

## 非表示カードの一時表示

ダッシュボードの`show_card`処理を特定のToDoカード専用ではなく、既知の非表示カード共通にしました。

条件:

- `show_card`が指定されている
- 指定値が現在の`hidden`に含まれる既知カードIDである

この場合だけ、その画面で対象カードを一時表示します。

未知のカードIDを指定しても追加表示しません。

これにより、時間カードを通常非表示にしている場合でもToday Focusから目標設定欄へ移動できます。

## 壊れた保存値への対応

`app_settings`に次のような不正値が残っていても画面を500にしません。

- JSON文字列`"120"`
- 真偽値
- 0以下
- 1441以上
- JSONとして読み取れない値

不正値は「目標未設定」として扱います。

## バックアップ

`app_settings`は現在のJSONバックアップ・復元対象外です。

そのため`daily_time_goal_minutes`もバックアップされません。

バックアップ復元後は時間記録などの実績データは復元されますが、1日の時間目標は再設定が必要です。

これは現行のダッシュボード設定・Undo情報と同じ扱いです。

将来`app_settings`をバックアップ対象へ含める場合は、短期Undoのような一時情報と永続設定を分離して扱う必要があります。

## セキュリティ・安全性

- サーバー側で1〜1440分を再検証
- HTMLの`min`/`max`だけに依存しない
- 不正入力で既存設定を上書きしない
- 保存値が壊れていても安全に未設定へフォールバック
- `show_card`は保存済みの既知カードIDとの一致時だけ一時表示
- 任意URLへのリダイレクトなし
- DBスキーマ変更なし
- 時間実績の更新処理は変更しない

## テスト観点

- 初期状態は目標未設定
- 目標設定後にSQLiteへ保持される
- 目標を変更できる
- 目標を解除できる
- 解除しても時間記録は維持される
- 1分・1440分を受け付ける
- 0以外の範囲外値を拒否する
- 空文字・文字列・小数を拒否する
- 不正入力で既存目標を維持する
- 95/120分を79%・残り25分と表示する
- 150/120分を125%・30分超過と表示する
- プログレスバーを100%で上限にする
- Today Focusへ進捗を反映する
- 時間カード非表示時も`show_card=time`で一時表示できる
- 未知の`show_card`でカードを追加表示しない
- 壊れた保存値で500にならない
- 内部サービスが`bool`を整数として保存しない

## 対象外

- 曜日ごとに異なる目標
- カテゴリ別目標
- 週次・月次の目標
- 目標達成通知
- 目標の履歴保存
- 目標達成日数の統計
- ストリーク
- JSONバックアップへの`app_settings`追加
