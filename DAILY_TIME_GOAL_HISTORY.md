# 1日の時間目標 履歴仕様

## 目的

現在値しか保存していなかった1日の時間目標を期間履歴として保持し、過去日の目標達成判定や達成ストリークを正しい当時の目標値で計算できる基盤を作る。

## データモデル

`daily_time_goal_periods`

- `id`: 主キー
- `goal_minutes`: 1〜1440分
- `started_on`: 適用開始日
- `ended_on`: 適用終了日。現在有効な期間はNULL
- `created_at`: 作成日時

開始日は一意とし、期間の重複を許可しない。目標を解除していた期間は履歴レコードを作らず、期間間の空白として表現する。

## 現在値との関係

既存UIとの互換性を維持するため、現在の目標値は引き続き `app_settings.daily_time_goal_minutes` に保持する。保存・解除時は現在値と期間履歴を同一トランザクションで更新する。

`app_settings` は現在値の読み取り用ミラー、`daily_time_goal_periods` は履歴の正本として扱う。

## 保存ルール

- 初回設定: 当日開始の開放期間を作る
- 同日中の変更: 同じ期間の `goal_minutes` を更新する
- 翌日以降に同じ値を保存: 新しい期間を作らない
- 翌日以降に値を変更: 旧期間を変更日の前日で閉じ、新期間を作る
- 解除: 開放期間を解除日の前日で閉じる
- 設定当日に解除: その日の期間を削除する
- 再設定: 新しい開始日で開放期間を作る

## 既存SQLiteの移行

`daily_time_goal_periods` が存在しないDBでは起動時に自動作成する。

既存の `app_settings.daily_time_goal_minutes` が有効な1〜1440の整数の場合だけ、その現在値を**移行日から**有効な履歴として1件作成する。過去にいつ設定されたかは既存データから判定できないため、過去期間を推測して生成しない。

壊れた設定値や文字列型の数値などは履歴へ移行しない。

## JSONバックアップ v7

スキーマバージョンを7へ更新し、`daily_time_goal_periods` をバックアップ対象へ追加する。

v7では各履歴について以下を保存する。

- `id`
- `goal_minutes`
- `started_on`
- `ended_on`
- `created_at`

検証では型、値域、日付、開始日重複、期間重複、レコード件数を確認する。

## 旧バックアップ互換性

v1〜v6は引き続き復元可能とする。ただし旧バックアップには時間目標の現在値・履歴が含まれていなかったため、復元後の時間目標履歴は空になる。存在しない過去情報は推測して補完しない。

## v7復元

v7復元では期間履歴を復元し、開放中の履歴が1件ある場合はその `goal_minutes` から `app_settings.daily_time_goal_minutes` を再構築する。

開放中期間がない場合は現在目標を未設定とする。開放中期間が複数ある不正データは検証または復元時に拒否する。

## 対象外

この変更では次を実装しない。

- 過去日の目標達成率表示
- 目標達成日数
- 目標達成ストリーク
- 目標履歴の編集
- 日付を指定した目標変更

これらは期間履歴を利用する後続機能として実装する。
