# 時間インサイト仕様

## 目的

時間記録カードで「今日何分取り組んだか」だけでなく、直近7日の活動量・継続状況・カテゴリ傾向をすぐ確認できるようにします。

既存の`time_entries`だけを参照する読み取り専用機能とし、専用テーブルやDBマイグレーションは追加しません。

## 表示場所

ダッシュボードの「学習/作業時間」カード内に表示します。

表示内容:

- 直近7日合計
- 活動日数
- 活動日平均
- 連続記録日数
- 前7日との合計時間比較
- 直近7日の最多カテゴリ
- 直近7日の日別推移
- 週次集計への導線

## 集計期間

今日を含む直近7日を現在期間とします。

例: 2026-08-17の場合

```text
現在期間: 2026-08-11〜2026-08-17
比較期間: 2026-08-04〜2026-08-10
```

未来日のデータは集計しません。

## 7日合計

現在期間に存在する`time_entries.minutes`の合計です。

同じ日に複数件ある場合はすべて合算します。

## 活動日

1分以上の時間記録が存在する日を1活動日として数えます。

表示例:

```text
5/7日
```

## 活動日平均

記録がある日の平均時間です。

```text
直近7日合計 / 活動日数
```

活動日が0日の場合は0分です。

7日すべてを分母にはしません。未記録日を含む平均ではなく「実際に取り組んだ日の平均」を表示します。

## 連続記録

1分以上の記録が存在する日を連続記録日として数えます。

### 今日に記録がある場合

今日から過去へ連続している日数を数えます。

### 今日がまだ未記録の場合

昨日から過去へ連続している日数を数えます。

これにより、朝や作業開始前の時点で前日までのストリークを即座に0へしません。

例:

```text
8/14 記録あり
8/15 記録あり
8/16 記録あり
8/17 まだ未記録
```

表示:

```text
連続記録 3日
今日はまだ未記録
```

### 昨日が未記録の場合

今日も未記録なら連続記録は0日です。

## 前7日比較

現在期間の合計と、その直前7日間の合計を比較します。

比較期間が0分の場合は0除算を行わず、割合を表示しません。

例:

```text
前7日 140分
直近7日 210分
→ 前7日比 +50%
```

前7日が0分の場合:

```text
前7日は0分・今期は210分
```

## 最多カテゴリ

現在期間のカテゴリ別合計で最も時間が長いカテゴリを表示します。

同率の場合は既存CRUDの並び順を利用し、カテゴリ名昇順で先に来るカテゴリを表示します。

## 日別推移

現在期間の7日を古い日付から今日まで表示します。

各日のプログレス値は、直近7日で最も記録時間が長い日を100として相対表示します。

この値は目標達成率ではありません。

## DBアクセス

時間カードが通常表示されている場合、または`show_card=time`で一時表示される場合だけ追加集計を実行します。

追加クエリ:

1. 現在7日 + 比較7日の14日分の日別合計
2. 今日以前の記録日一覧（連続記録計算用）
3. 現在7日のカテゴリ別合計

時間カードが非表示で、一時表示もされていない場合はこの3クエリを実行しません。

## アクセシビリティ

- 集計指標のまとまりへ`aria-label`を設定
- 日別推移は`ul`で時系列として表現
- 各`progress`へ日付と実時間を含む`aria-label`を設定
- 色だけに依存せず、分数・割合・日数をテキストで表示

## セキュリティ・安全性

- 既存の時間記録を読み取るだけで更新しない
- ユーザー入力を追加で受け付けない
- 新しいAPIエンドポイントを追加しない
- DBスキーマを変更しない
- テンプレートへ渡すカテゴリ名はJinja2の自動エスケープ対象
- 未来日データをストリーク計算対象へ含めない

## パフォーマンス

- 14日の日別集計は期間を限定する
- カテゴリ集計も7日に限定する
- ストリークは重複を除いた記録日のみ取得する
- 時間カード非表示時は追加集計自体をスキップする

個人利用・SQLite前提では十分に軽量な構成とします。

記録日数が大きく増えた場合は、ストリーク計算用クエリを連続期間だけ取得する方式へ最適化する余地があります。

## テスト観点

- データなしで0状態を表示
- 直近7日だけを現在期間へ含める
- 8〜14日前を比較期間へ含める
- 15日以上前を比較へ含めない
- 活動日平均を記録日のみで算出
- 前7日0分でも0除算しない
- 今日記録ありの連続日数
- 今日未記録でも昨日までの連続日数を維持
- 昨日未記録ならストリーク0
- 同日の複数記録を1活動日として扱う
- 未来日の記録をストリークへ含めない
- 最多カテゴリを表示
- 日別推移を7件表示
- 時間カード非表示時はインサイトを描画しない
- `show_card=time`では一時表示できる

## 対象外

- 目標達成日数
- 過去の時間目標との相関
- 曜日別目標
- カテゴリ別目標
- タイマーセッション履歴
- 年単位の分析
- AIによる振り返り文章生成
