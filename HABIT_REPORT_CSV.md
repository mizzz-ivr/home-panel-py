# Home Panel 習慣レポートCSV仕様

## 目的

習慣の週次・月次集計をCSVとして保存し、表計算ソフトでの分析、定期的な振り返り、長期保存に利用できるようにします。

CSV専用の達成率計算は行いません。画面と同じ`build_period_report()`の結果を利用し、次の条件を一貫して反映します。

```text
達成対象 = 習慣の有効期間内 AND 曜日設定期間内 AND 対象曜日
```

以下は対象件数へ含めません。

- 習慣作成前
- 習慣終了後
- 終了から再開までの停止期間
- 対象曜日外
- 未来日

## URL

### 週次CSV

```text
GET /habits/weekly.csv?target_date=YYYY-MM-DD
```

- 指定日を含む月曜日〜日曜日を出力
- `target_date`未指定時は現在週
- 未来日指定は400
- ファイル名の日付は週の開始日

```text
home-panel-habit-weekly-YYYY-MM-DD.csv
```

### 月次CSV

```text
GET /habits/monthly.csv?target_month=YYYY-MM
```

- 指定月の1日〜月末を出力
- `target_month`未指定時は現在月
- 未来月指定は400

```text
home-panel-habit-monthly-YYYY-MM.csv
```

## 画面からの操作

週次・月次画面には、表示中の期間を出力するリンクがあります。

- 週次: `この週をCSV出力`
- 月次: `この月をCSV出力`

表示中の週開始日または月をクエリへ明示し、画面とCSVの対象期間を一致させます。

## ファイル形式

- UTF-8 BOM付き
- 改行コードはCRLF
- Python標準`csv.writer`でエスケープ
- MIMEタイプは`text/csv; charset=utf-8`
- ファイルはダウンロードとして返却

1ファイルは次の3セクションで構成します。

1. 概要
2. 日別集計
3. 習慣別集計

各セクションの間には空行を1行入れます。

## 概要

| 項目 | 内容 |
|---|---|
| レポート種別 | 習慣週次レポートまたは習慣月次レポート |
| 対象期間 | 画面上の週開始〜週終了、または月初〜月末 |
| 集計終了日 | 未来日を除外した実際の集計最終日 |
| 達成数 | 対象条件を満たす達成記録数 |
| 対象件数 | 習慣と対象日の組み合わせ数 |
| 達成率（%） | 達成数 ÷ 対象件数。対象0件の場合は0 |
| 全習慣達成日 | 対象習慣が1件以上あり、全件達成した日数 |

## 日別集計

列:

| 列 | 内容 |
|---|---|
| 日付 | `YYYY-MM-DD` |
| 曜日 | 月〜日 |
| 状態 | `集計済み`または`未到来` |
| 達成数 | その日の対象内達成数 |
| 対象件数 | その日の対象習慣数 |
| 達成率（%） | その日の達成率 |

現在週・現在月の未来日も行として残します。

未来日は次の形式です。

```csv
2026-07-27,月,未到来,,,
```

未来日を0件・0%として出力すると未達成と誤解されるため、数値列は空欄にします。

対象曜日外や停止期間によって、その日に対象習慣がない場合は集計済みとして次のように出力します。

```csv
2026-07-21,火,集計済み,0,0,0
```

## 習慣別集計

列:

| 列 | 内容 |
|---|---|
| 習慣名 | 現在の習慣名 |
| 集計終了日時点の対象曜日 | 集計終了日に適用される曜日設定 |
| 達成日数 | 選択期間内の対象日に達成した日数 |
| 対象日数 | 有効期間と曜日設定を満たす日数 |
| 達成率（%） | 達成日数 ÷ 対象日数 |
| 最長連続回数 | 対象日ベースの最長連続達成回数 |
| 現在状態 | `利用中`または`終了済み` |

曜日設定は期間中に変更される可能性があります。対象日数と達成率は各日時点の設定履歴で計算しますが、曜日表示は列名のとおり集計終了日時点の設定です。

内部実装の有効期間件数や曜日設定期間件数は、選択期間内の件数と誤解される可能性があるためCSVへ出力しません。

## CSV数式注入対策

習慣名などの文字列セルが次の文字から始まる場合、先頭へアポストロフィを付与します。

```text
= + - @ TAB CR LF
```

先頭に空白があり、その後に`=`, `+`, `-`, `@`が続く場合も対象です。

例:

```text
  =SUM(1,1)
```

CSV上では次のように出力します。

```text
'  =SUM(1,1)
```

この処理は既存の時間記録CSVと共通の`sanitize_csv_cell()`を利用します。

## HTTPヘッダー

成功時:

```text
Content-Disposition: attachment; filename="...csv"
Cache-Control: no-store
X-Content-Type-Options: nosniff
```

入力エラー時も次を設定します。

```text
Cache-Control: no-store
X-Content-Type-Options: nosniff
```

エラー本文はUTF-8のプレーンテキストです。

## 空データ

習慣が1件もない場合も200でCSVを返します。

- 概要は達成数0・対象件数0・達成率0
- 日別集計は対象期間の日付を出力
- 習慣別集計はヘッダーだけを出力

空データでも列構成を変えないため、自動処理で同じ形式として扱えます。

## データ更新

CSV出力は読み取り専用です。

- 習慣を変更しない
- 達成記録を追加・削除しない
- 有効期間を変更しない
- 曜日設定期間を変更しない
- DBスキーマを変更しない

## テスト観点

- UTF-8 BOM
- CRLF
- Content-Type
- Content-Dispositionとファイル名
- no-store / nosniff
- 週次・月次の対象期間
- パラメータ未指定時の現在期間
- 概要・日別・習慣別の列構成
- 対象曜日外の日
- 現在期間の未来日
- 空DB
- CSV数式注入対策
- 不正日付・不正月
- 未来日・未来月
- CSV出力前後で達成記録数が変わらないこと
- 週次・月次画面のリンク
- 既存HTMLレポートと集計サービスの共通利用

## 未対応

- 日別履歴CSV
- 年次レポートCSV
- 複数週・複数月をまとめた出力
- CSVインポート
- CSVからの復元
- タイムゾーン設定
- 認証・ユーザー分離
