# ToDo期限・優先度仕様

## 目的

ToDoへ期限と優先度を設定し、今対応すべきタスクをダッシュボード上で判断しやすくします。

既存のタイトル・完了切り替え・削除操作は維持し、期限や優先度を指定しない従来運用も継続できます。

## データ項目

`tasks`へ次の項目を追加します。

| 項目 | 型 | 必須 | 既定値 | 説明 |
|---|---|---:|---|---|
| `due_date` | `DATE` | いいえ | `null` | タスクの期限日 |
| `priority` | `VARCHAR(10)` | はい | `medium` | `low`・`medium`・`high` |

### 優先度

| 保存値 | 表示 |
|---|---|
| `low` | 低 |
| `medium` | 中 |
| `high` | 高 |

アプリケーションの入力検証と新規DBのCHECK制約で、上記以外を拒否します。

SQLiteの既存テーブルへ列を追加する軽量移行では、テーブル再作成を避けるためCHECK制約自体は追加しません。既存DBについては移行時の正規化とアプリケーション側の入力検証で不正値を防ぎます。

## 既存SQLiteの移行

Web起動や`Base.metadata.create_all()`の実行時に、次を冪等に行います。

1. `tasks.due_date`がなければ追加
2. `tasks.priority`がなければ`medium`を既定値として追加
3. `priority`がnullまたは不正値の場合は`medium`へ補正
4. `due_date`・`priority`のインデックスを作成

既存タスクは次の状態になります。

```text
due_date = null
priority = medium
```

タスクID、タイトル、完了状態、作成日時、更新日時は変更しません。

## 作成

### 旧ルート

```text
POST /tasks
```

タイトルのみを受け付ける既存ルートです。後方互換のため維持します。

作成される値:

```text
due_date = null
priority = medium
```

### 詳細指定ルート

```text
POST /tasks/advanced
```

フォーム項目:

```text
title=<1〜255文字>
due_date=<YYYY-MM-DD または空>
priority=low|medium|high
```

成功時は作成したタスクのアンカーへ303リダイレクトします。

```text
/#task-<ID>
```

## 編集

```text
POST /tasks/{task_id}/details
```

フォーム項目:

```text
due_date=<YYYY-MM-DD または空>
priority=low|medium|high
```

- 空の期限は`null`として保存
- 存在しないタスクは404
- 不正日付・不正優先度は400
- タイトル・完了状態は変更しない

## 一覧の並び順

未完了タスクを期限状態で分類します。

1. 期限切れ
2. 本日期限
3. 将来期限
4. 期限なし
5. 完了済み

未完了の同じ期限状態内では次の順です。

1. 優先度: 高 → 中 → 低
2. 期限日: 早い順
3. 作成日時: 新しい順
4. ID: 大きい順

完了済みタスクは期限切れとして警告せず、一覧下部へまとめます。

## 表示

未完了タスク:

- 期限切れ: 赤系の背景・境界線
- 本日期限: 黄系の背景・境界線
- 将来期限: 通常表示
- 期限なし: `期限なし`
- 優先度: 低・中・高のバッジ

完了済みタスク:

- タイトルへ取り消し線
- 過去期限でも期限切れ警告クラスを付けない
- 保存済みの期限・優先度は表示・編集可能

期限・優先度の編集フォームは各タスクの`details`内へ格納し、通常時の画面密度を抑えます。

## 横断検索

ToDoの検索対象は従来どおりタイトルです。

検索結果には次を表示します。

- 完了・未完了
- 優先度
- 期限または期限なし
- 更新日時

検索結果URL:

```text
/?show_card=todo#task-<ID>
```

ToDoカードが保存設定で非表示の場合も、検索結果を開いた画面だけ一時表示します。保存設定は変更しません。

## JSONバックアップv6

v6のToDoレコード:

```json
{
  "id": 1,
  "title": "リリース準備",
  "is_done": false,
  "due_date": "2026-08-10",
  "priority": "high",
  "created_at": "2026-08-06T01:00:00Z",
  "updated_at": "2026-08-06T01:00:00Z"
}
```

期限なしの場合:

```json
{
  "due_date": null,
  "priority": "medium"
}
```

### 検証

- `due_date`: `null`または実在する`YYYY-MM-DD`
- `priority`: `low`・`medium`・`high`
- 未知項目と不足項目を拒否
- v1〜v5の従来ToDo構造は引き続き検証可能

### 復元

- v6: 期限・優先度をそのまま復元
- v1〜v5: `due_date=null`・`priority=medium`を補完してv6へ正規化
- 一時DBへの投入、外部キー確認、SQLite整合性確認、再エクスポート一致確認を継続
- 非空DBへは復元しない

## テスト観点

- 旧作成ルートの後方互換
- 期限・優先度付き作成
- 期限・優先度の更新
- 期限を未指定へ戻す操作
- 不正な日付・優先度
- 存在しないタスク
- 期限状態・優先度・期限日による並び順
- 完了済みタスクの期限警告除外
- モバイル向け1列フォーム
- 横断検索表示・リンク
- SQLite移行と冪等性
- バックアップv6の出力・検証・復元
- v1〜v5の既定値補完
- 復元失敗時の既存DB維持と一時ファイル削除
- 既存ToDo・メモ・時間記録・習慣機能の回帰

## 対象外

- 期限通知・リマインダー
- 繰り返しToDo
- タスク名編集
- 完了日時・完了履歴
- ラベル・プロジェクト分類
- ドラッグによるToDo順序変更
- 認証・ユーザー分離
