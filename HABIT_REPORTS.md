# Home Panel 習慣レポート仕様

## 目的

習慣トラッカーで保存した達成記録を、日別・週次・月次で振り返ります。

確認できる内容:

- 指定日の達成・未達成
- 期間全体の達成数と達成率
- 全対象習慣を達成した日数
- 日別達成率
- 習慣別達成日数
- 期間内の最長連続日数
- 終了済み習慣の過去実績
- 終了・再開を繰り返した習慣の停止期間除外

## URL

| 画面 | URL | パラメータ |
|---|---|---|
| 日別履歴 | `/habits/history` | `target_date=YYYY-MM-DD` |
| 週次集計 | `/habits/weekly` | `target_date=YYYY-MM-DD` |
| 月次集計 | `/habits/monthly` | `target_month=YYYY-MM` |

未来日・未来月は指定できません。

## 達成率の計算

達成率は次の式で計算します。

```text
達成率 = 達成した対象件数 ÷ 期間中の対象件数 × 100
```

期間中の対象件数は、各日に有効だった習慣数の合計です。

例:

- 月曜日は習慣2件
- 火曜日に新しい習慣を追加して3件
- 月曜日に2件達成
- 火曜日に2件達成

```text
達成数 = 4件
対象件数 = 2件 + 3件 = 5件
達成率 = 80%
```

新しい習慣の作成前、終了後、停止中の日を未達成として扱わないため、単純な「現在の習慣数 × 日数」では計算しません。

## 習慣の有効期間

習慣が達成対象だった期間は`habit_active_periods`へ保存します。

```text
started_on <= 対象日 <= ended_on
```

`ended_on`がnullの場合は継続中です。

### アクティブな習慣

開放中の有効期間が1件あり、その開始日以降を対象にします。

### 終了済み習慣

すべての有効期間が終了済みです。各区間の終了日の翌日以降は対象外です。

終了済みでも、その習慣が有効だった過去の日・週・月では表示します。

### 終了後に再開した習慣

終了済み区間を保持したまま、新しい開放区間を追加します。

例:

```text
第1区間: 7月1日〜3日
停止中: 7月4日〜5日
第2区間: 7月6日〜
```

7月4日・5日は対象件数へ含めません。

同日に終了・再開した場合は日単位の停止期間が存在しないため、直前区間を再オープンします。

### 現在期間の未来日

現在週・現在月に含まれる未来日は分母へ含めません。

### 互換フォールバック

移行途中などで習慣に有効期間履歴がまだ存在しない場合に限り、以下を利用します。

- 開始日: `created_at`
- 終了日: `archived_at`
- `archived_at`がない旧データ: `updated_at`

通常の集計では`habit_active_periods`を正とします。

## 日別履歴

指定日に有効だった習慣を表示します。

- 達成
- 未達成
- 終了済み表示
- 達成数
- 達成率

停止期間中の習慣は、異常な達成記録が存在しない限り表示しません。

読み取り専用で、過去日の達成状態は変更しません。

## 週次集計

指定日を含む月曜日〜日曜日を表示します。

- 期間全体の達成数・達成率
- 全習慣達成日
- 日別達成率
- 習慣別達成日数
- 習慣別最長連続日数

現在週では未来日を「未到来」として表示します。

## 月次集計

指定月をカレンダー形式で表示します。

- 日別達成数
- 日別対象件数
- 日別達成率
- 日別履歴へのリンク
- 習慣別達成日数・達成率
- 習慣別最長連続日数

## 最長連続日数

選択期間の範囲内だけで計算します。

月をまたいで連続していても、月次画面では選択月内の連続日数のみ表示します。

停止期間を挟んだ場合はカレンダー日付が連続しないため、停止前後を1つの連続記録として結合しません。

ダッシュボードの「現在の連続日数」とは目的が異なります。

- ダッシュボード: 現在まで継続している日数
- レポート: 選択期間内で最も長く連続した日数

## 不整合データの扱い

達成記録が有効期間外に存在する場合、日別画面では記録の存在を確認できることがありますが、達成率の分子・分母には含めません。

JSONバックアップv4の検証CLIでは、有効期間外の達成記録をエラーとして検出します。

## 空データ

対象習慣がない日・週・月でも画面を表示します。

- 達成数: `0/0件`
- 達成率: `0%`
- 習慣別一覧: 空状態メッセージ

0除算は行いません。

## 性能

現状は個人利用を前提とし、全習慣・全有効期間を取得して期間内の日数ごとに有効状態を計算します。

アクティブ習慣は最大20件ですが、終了済み習慣と有効期間は蓄積します。長期運用で件数が増えた場合は、次を検討します。

- 対象期間と交差する有効期間だけをSQLで取得
- 集計結果のキャッシュ
- 終了済み習慣のページング
- 年単位の集計テーブル

## 移行上の制約

有効期間テーブル導入前は、複数回の終了・再開履歴を保存していません。

既存データ移行では、各習慣を次の1区間へ変換します。

- アクティブ習慣: 作成日〜継続中
- 終了済み習慣: 作成日〜直近終了日

過去の停止区間を推測して復元することはできません。

## 未対応

- 過去日の達成編集
- 有効期間の手動編集
- 習慣レポートCSV
- 曜日指定・週N回を考慮した達成率
- 年次集計
- タイムゾーン設定
