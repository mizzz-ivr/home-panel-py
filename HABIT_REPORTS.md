# Home Panel 習慣レポート仕様

## 目的

習慣トラッカーで保存した達成記録を、有効期間と対象曜日を考慮して日別・週次・月次で振り返ります。

確認できる内容:

- 指定日の達成・未達成・対象外
- 期間全体の達成数と達成率
- 全対象習慣を達成した日数
- 日別達成率
- 習慣別達成日数
- 期間内の最長連続値
- 終了済み習慣の過去実績
- 終了・再開による停止期間の除外
- 曜日設定変更前後の条件を保持した集計

## URL

| 画面 | URL | パラメータ |
|---|---|---|
| 日別履歴 | `/habits/history` | `target_date=YYYY-MM-DD` |
| 週次集計 | `/habits/weekly` | `target_date=YYYY-MM-DD` |
| 月次集計 | `/habits/monthly` | `target_month=YYYY-MM` |

未来日・未来月は指定できません。

## 達成率の計算

```text
達成率 = 達成した対象件数 ÷ 期間中の対象件数 × 100
```

対象件数は、各日について以下の両方を満たす習慣数の合計です。

1. `habit_active_periods`の有効期間内
2. `habit_schedule_periods`の対象曜日

例:

```text
月・水・金の習慣
月: 達成
火: 対象外
水: 未達成
木: 対象外
金: 達成
```

```text
達成数 = 2件
対象件数 = 3件
達成率 = 67%
```

火曜・木曜は分子にも分母にも含めません。

新しい習慣の作成前、終了後、停止中、対象曜日外、未来日を未達成として扱わないため、単純な「現在の習慣数 × 日数」では計算しません。

## 習慣の有効期間

習慣が利用中だった期間は`habit_active_periods`へ保存します。

```text
started_on <= 対象日 <= ended_on
```

`ended_on`がnullの場合は継続中です。

### アクティブな習慣

開放中の有効期間が1件あり、その開始日以降を利用中として扱います。

### 終了済み習慣

すべての有効期間が終了済みです。各区間の終了日の翌日以降は対象外です。

終了済みでも、有効だった過去の日・週・月では表示します。

### 終了後に再開した習慣

終了済み区間を保持したまま、新しい開放区間を追加します。

```text
第1区間: 7月1日〜3日
停止中: 7月4日〜5日
第2区間: 7月6日〜
```

7月4日・5日は対象件数へ含めません。

同日に終了・再開した場合は日単位の停止期間が存在しないため、直前区間を再オープンします。

## 対象曜日の適用期間

曜日設定は`habit_schedule_periods`へ保存します。

```text
7月1日〜9日: 毎日
7月10日〜: 月・水・金
```

- 7月9日以前: 毎日として集計
- 7月10日以降: 月・水・金だけ集計

曜日設定を変更しても、変更前のレポートは新設定で再計算しません。

対象判定:

```text
有効期間内 AND 曜日設定期間内 AND 対象曜日
```

終了済み習慣も曜日設定履歴を保持します。終了中に新しい曜日を保存した場合、その設定は再開後の対象判定に利用します。

## 現在期間の未来日

現在週・現在月に含まれる未来日は分母へ含めません。

## 互換フォールバック

移行途中などで有効期間履歴がまだ存在しない場合に限り、以下を利用します。

- 開始日: `created_at`
- 終了日: `archived_at`
- `archived_at`がない旧データ: `updated_at`

曜日設定履歴が存在しない場合は「毎日」として扱います。

通常の集計では`habit_active_periods`と`habit_schedule_periods`を正とします。

## 日別履歴

指定日に利用中だった習慣を表示します。

- 達成
- 未達成
- 対象曜日外
- 終了済み表示
- 達成数
- 達成率
- その日に適用されていた曜日設定

対象曜日外の習慣は状況確認のため表示しますが、達成数・対象件数・達成率には含めません。

有効期間外の習慣は、異常な達成記録が存在しない限り表示しません。

読み取り専用で、過去日の達成状態や曜日設定は変更しません。

## 週次集計

指定日を含む月曜日〜日曜日を表示します。

- 期間全体の達成数・達成率
- 全習慣達成日
- 日別達成率
- 習慣別達成日数
- 習慣別最長連続値
- 習慣ごとの現在の曜日設定

現在週では未来日を「未到来」として表示します。

## 月次集計

指定月をカレンダー形式で表示します。

- 日別達成数
- 日別対象件数
- 日別達成率
- 日別履歴へのリンク
- 習慣別達成日数・達成率
- 習慣別最長連続値
- 習慣ごとの現在の曜日設定

## 最長連続値

選択期間の範囲内だけで計算します。

毎日設定ではカレンダー日の連続達成です。曜日指定設定では、対象日の連続達成回数です。

例:

```text
対象: 月・水・金
月: 達成
水: 達成
金: 達成
```

火曜・木曜は対象外なので連続を切らず、連続値は3です。

月をまたいで連続していても、月次画面では選択月内の対象日だけで計算します。

停止期間は対象日の一覧から除外します。停止前後に対象日が存在する場合でも、利用区間が分かれるため、集計の意味を確認できるよう有効期間数を内部結果へ保持します。

画面上は既存互換のため「日連続」と表示しますが、曜日指定習慣では対象日の連続回数を表します。

## 不整合データの扱い

達成記録が有効期間外または対象曜日外に存在する場合、日別画面では記録の存在を確認できることがありますが、達成率の分子・分母には含めません。

新しい達成操作では、対象曜日外の登録をサーバー側で拒否します。既存の不整合記録は取り消しできます。

JSONバックアップv5の検証CLIでは、以下をエラーとして検出します。

- 有効期間外の達成記録
- 曜日設定期間外の達成記録
- 対象曜日外の達成記録
- 曜日設定期間の重複・交差
- 開放中の曜日設定期間数の不整合

## 空データ

対象習慣がない日・週・月でも画面を表示します。

- 達成数: `0/0件`
- 達成率: `0%`
- 習慣別一覧: 空状態メッセージ

0除算は行いません。

## 性能

現状は個人利用を前提とし、全習慣・有効期間・曜日設定期間を取得して、期間内の日数ごとに対象状態を計算します。

アクティブ習慣は最大20件ですが、終了済み習慣と各種期間は蓄積します。長期運用で件数が増えた場合は、次を検討します。

- 対象期間と交差する期間だけをSQLで取得
- 集計結果のキャッシュ
- 終了済み習慣のページング
- 年単位の集計テーブル
- 曜日マスクを利用したSQL側の事前絞り込み

## 移行上の制約

有効期間テーブル導入前は、複数回の終了・再開履歴を保存していません。既存データは次の1区間へ変換します。

- アクティブ習慣: 作成日〜継続中
- 終了済み習慣: 作成日〜直近終了日

曜日設定テーブル導入前は曜日変更履歴を保存していません。既存習慣は作成日から「毎日」として移行します。

過去の停止区間や曜日変更を推測して復元することはできません。

## 未対応

- 過去日の達成編集
- 有効期間・曜日設定期間の手動編集
- 習慣レポートCSV
- 曜日を固定しない週N回
- 隔週・月次スケジュール
- 年次集計
- タイムゾーン設定
