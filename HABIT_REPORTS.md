# Home Panel 習慣レポート仕様

## 目的

習慣トラッカーで保存した達成記録を、有効期間と対象曜日を考慮して日別・週次・月次で振り返ります。

確認できる内容:

- 指定日の達成・未達成・対象外
- 過去日の達成状態修正
- 対象日の一括達成・一括取り消し
- 選択した習慣だけの一括達成・一括取り消し
- 直前の達成操作を元に戻すUndo
- 期間全体の達成数と達成率
- 全対象習慣を達成した日数
- 日別達成率
- 習慣別達成日数
- 期間内の最長連続値
- 終了済み習慣の過去実績
- 終了・再開による停止期間の除外
- 曜日設定変更前後の条件を保持した集計

## URL

| 画面・出力・操作 | URL | パラメータ |
|---|---|---|
| 日別履歴 | `/habits/history` | `target_date=YYYY-MM-DD` |
| 達成状態更新 | `/habits/{habit_id}/completion` | POST: `target_date`, `completed` |
| 達成状態全件一括更新 | `/habits/completions/bulk` | POST: `target_date`, `action` |
| 達成状態選択一括更新 | `/habits/completions/selected` | POST: `target_date`, `completed`, `habit_ids` |
| Undo状態取得 | `/habits/completions/undo` | GET: `target_date` |
| Undo実行 | `/habits/completions/undo` | POST: `token` |
| 週次集計 | `/habits/weekly` | `target_date=YYYY-MM-DD` |
| 月次集計 | `/habits/monthly` | `target_month=YYYY-MM` |
| 週次CSV | `/habits/weekly.csv` | `target_date=YYYY-MM-DD` |
| 月次CSV | `/habits/monthly.csv` | `target_month=YYYY-MM` |

未来日・未来月は指定できません。

## 達成率の計算

```text
達成率 = 達成した対象件数 ÷ 期間中の対象件数 × 100
```

対象件数は、各日について以下の両方を満たす習慣数の合計です。

1. `habit_active_periods`の有効期間内
2. `habit_schedule_periods`の対象曜日

例:

```text
月・水・金の習慣
月: 達成
火: 対象外
水: 未達成
木: 対象外
金: 達成
```

```text
達成数 = 2件
対象件数 = 3件
達成率 = 67%
```

火曜・木曜は分子にも分母にも含めません。

新しい習慣の作成前、終了後、停止中、対象曜日外、未来日を未達成として扱わないため、単純な「現在の習慣数 × 日数」では計算しません。

## 習慣の有効期間

習慣が利用中だった期間は`habit_active_periods`へ保存します。

```text
started_on <= 対象日 <= ended_on
```

`ended_on`がnullの場合は継続中です。

### アクティブな習慣

開放中の有効期間が1件あり、その開始日以降を利用中として扱います。

### 終了済み習慣

すべての有効期間が終了済みです。各区間の終了日の翌日以降は対象外です。

終了済みでも、有効だった過去の日・週・月では表示し、対象日だった過去の達成状態を修正できます。

### 終了後に再開した習慣

終了済み区間を保持したまま、新しい開放区間を追加します。

```text
第1区間: 7月1日〜3日
停止中: 7月4日〜5日
第2区間: 7月6日〜
```

7月4日・5日は対象件数へ含めず、新しい達成記録も追加できません。

同日に終了・再開した場合は日単位の停止期間が存在しないため、直前区間を再オープンします。

## 対象曜日の適用期間

曜日設定は`habit_schedule_periods`へ保存します。

```text
7月1日〜9日: 毎日
7月10日〜: 月・水・金
```

- 7月9日以前: 毎日として集計
- 7月10日以降: 月・水・金だけ集計

曜日設定を変更しても、変更前のレポートは新設定で再計算しません。

対象判定:

```text
有効期間内 AND 曜日設定期間内 AND 対象曜日
```

終了済み習慣も曜日設定履歴を保持します。終了中に新しい曜日を保存した場合、その設定は再開後の対象判定に利用します。

## 現在期間の未来日

現在週・現在月に含まれる未来日は分母へ含めません。

未来日の達成状態は更新できません。ルートと達成更新サービスの両方で拒否します。

## 互換フォールバック

移行途中などで有効期間履歴がまだ存在しない場合に限り、以下を利用します。

- 開始日: `created_at`
- 終了日: `archived_at`
- `archived_at`がない旧データ: `updated_at`

曜日設定履歴が存在しない場合は「毎日」として扱います。

日別集計、単件達成、全件・選択一括達成は同じ対象判定を利用します。通常は`habit_active_periods`と`habit_schedule_periods`を正とし、履歴が存在しない場合だけ互換フォールバックを利用します。

## 日別履歴

指定日に利用中だった習慣を表示します。

- 達成
- 未達成
- 対象曜日外
- 終了済み表示
- 達成数
- 達成率
- その日に適用されていた曜日設定
- 達成状態の追加・取り消し
- 対象習慣の全件一括達成
- 指定日の達成記録の全件一括取り消し
- 選択した習慣だけの一括達成・一括取り消し
- 直前操作のUndo通知

対象曜日外の習慣は状況確認のため表示しますが、達成数・対象件数・達成率には含めません。

有効期間外の習慣は、異常な達成記録が存在しない限り表示しません。

### 達成にする

対象日かつ未達成の習慣に`達成にする`を表示します。

追加条件:

```text
対象日 <= 今日
AND 習慣の有効期間内
AND 曜日設定期間内
AND 対象曜日
```

次の場合は400で拒否します。

- 未来日
- 習慣作成前
- 終了後
- 終了から再開までの停止期間
- 対象曜日外
- 不正な日付
- `true` / `false`以外の状態

存在しない習慣は404です。

### 達成を取り消す

既存の達成記録がある場合は`達成を取り消す`を表示します。

対象曜日外や有効期間外に残っている不整合記録も修復できるよう、取り消し時は既存記録があれば削除できます。ただし未来日の編集は拒否します。

### 明示的な状態設定

過去日編集はトグルではなく、希望する最終状態を明示します。

```text
completed=true  -> 達成済みにする
completed=false -> 未達成にする
```

同じ操作を再送しても状態は反転しません。

```text
trueを2回送信  -> 達成記録は1件
falseを2回送信 -> 達成記録は0件
```

成功時は選択した日付の日別履歴へ303リダイレクトします。リダイレクト先はサーバー側で生成し、任意URLは受け付けません。

### 対象習慣をすべて達成

未達成の対象習慣がある場合に表示します。

一括達成では、指定日に有効期間・曜日設定・対象曜日を満たす未達成習慣だけを追加します。

```text
action=complete_expected
```

対象曜日外、停止期間、作成前、終了後の習慣へ達成記録は追加しません。対象外に既存の不整合記録がある場合も、一括達成では削除・変更しません。

対象習慣をセッションへ追加した後、達成記録本体を1回だけコミットします。同時実行で一意制約競合が発生した場合はロールバックして現在状態を再取得し、不足分だけを再追加します。

### この日の達成をすべて取り消す

指定日に達成記録が1件以上ある場合に表示します。

```text
action=clear_all
```

正常な記録だけでなく、対象曜日外・有効期間外の不整合記録も含め、その日の全達成記録を1トランザクションで削除します。

破壊的操作のため、フォームの`data-confirm`を共通JavaScriptが読み取り、送信前に確認ダイアログを表示します。JavaScriptが無効でもPOSTは可能なため、安全性はサーバー側の日付・操作種別検証に依存します。

### 習慣を選んで操作

対象習慣または達成記録がある場合に表示します。

```text
completed=true|false
habit_ids=<習慣ID>（複数指定）
```

- `completed=true`: 選択したすべての習慣が指定日の達成対象である場合だけ、未達成分を追加
- `completed=false`: 選択した習慣のうち、指定日に存在する達成記録だけを削除
- 選択なし・ID重複・不正IDは400
- 未知IDを含む場合は404
- 対象外習慣を達成追加へ混在させた場合は400
- 1件でも不正な選択があれば、正常な項目も更新しない
- 達成記録本体の追加・削除は1回のコミットで確定

チェックボックスは`form`属性で選択操作フォームへ関連付け、既存の単件操作フォームと入れ子にしません。

対象曜日外で記録がない項目にはチェックボックスを表示しません。対象外の既存記録にはチェックボックスを表示し、選択取り消しによる修復を可能にします。

### 一括操作の表示条件

| 状態 | 表示 |
|---|---|
| 未達成の対象習慣がある | 全件一括達成 |
| 達成記録が1件以上ある | 全件一括取り消し |
| 対象習慣または達成記録がある | 選択一括操作 |
| 対象習慣も達成記録もない | 一括操作セクションを非表示 |
| 全対象習慣が達成済み | 全件一括達成を非表示 |

全件・選択一括達成と取り消しは、いずれも冪等です。

詳細は[`HABIT_BULK_COMPLETION.md`](./HABIT_BULK_COMPLETION.md)を参照してください。

## 達成操作のUndo

ダッシュボードと日別履歴の達成操作では、状態が実際に変更された場合だけ直前1操作のUndoを保存します。

対象:

- ダッシュボード当日トグル
- 単件達成・取り消し
- 全件一括達成・取り消し
- 選択一括達成・取り消し

基本条件:

- 有効期限は10分
- 保存するのは直前1操作のみ
- 操作前・操作後の対象日達成集合を保存
- 現在集合が操作後集合と完全一致する場合だけ復元
- 後続変更がある場合は409で拒否
- 曜日設定変更・終了・再開で失効

達成記録本体は従来の1コミットで確定します。Undoメタデータは別Sessionへbest-effort保存し、保存失敗時も達成操作を維持します。Undo復元時は、達成記録の操作前集合への置換とUndo消費を同じコミットで行います。

一括取り消し前に不整合記録が存在していた場合も、操作前状態として復元します。Undoは整合性修復ではなく、操作前状態への回復機能です。

通知表示にはJavaScriptを使用しますが、トークン・期限・状態・参照整合性はPOST時にサーバー側で再検証します。

Pending Undoは一時情報のため、JSONバックアップv5へ含めません。

詳細は[`HABIT_COMPLETION_UNDO.md`](./HABIT_COMPLETION_UNDO.md)を参照してください。

## 週次集計

指定日を含む月曜日〜日曜日を表示します。

- 期間全体の達成数・達成率
- 全習慣達成日
- 日別達成率
- 習慣別達成日数
- 習慣別最長連続値
- 習慣ごとの現在の曜日設定

現在週では未来日を「未到来」として表示します。

日別履歴で達成状態を個別または一括変更すると、次回表示時に週次集計へ反映されます。Undoを実行した場合も、復元後の状態が反映されます。

## 月次集計

指定月をカレンダー形式で表示します。

- 日別達成数
- 日別対象件数
- 日別達成率
- 日別履歴へのリンク
- 習慣別達成日数・達成率
- 習慣別最長連続値
- 習慣ごとの現在の曜日設定

日別履歴で達成状態を個別または一括変更すると、次回表示時に月次集計へ反映されます。Undoを実行した場合も、復元後の状態が反映されます。

## 週次・月次CSV

週次・月次画面から、表示中の集計結果をCSVとしてダウンロードできます。

- 週次: `この週をCSV出力`
- 月次: `この月をCSV出力`

CSV専用の達成率計算は行わず、HTML画面と同じ`build_period_report()`の結果を利用します。

CSVは次の3セクションで構成します。

1. 概要
2. 日別集計
3. 習慣別集計

現在週・現在月の未来日は`未到来`として残し、達成数・対象件数・達成率を空欄にします。0件・0%とは区別します。

習慣別の曜日列は、期間中に設定変更があり得るため、`集計終了日時点の対象曜日`として出力します。対象日数と達成率は各日時点の曜日設定履歴を反映します。

CSVはUTF-8 BOM・CRLFで生成し、習慣名を含む文字列セルへ数式注入対策を適用します。

詳細は[`HABIT_REPORT_CSV.md`](./HABIT_REPORT_CSV.md)を参照してください。

## 最長連続値

選択期間の範囲内だけで計算します。

毎日設定ではカレンダー日の連続達成です。曜日指定設定では、対象日の連続達成回数です。

例:

```text
対象: 月・水・金
月: 達成
水: 達成
金: 達成
```

火曜・木曜は対象外なので連続を切らず、連続値は3です。

月をまたいで連続していても、月次画面では選択月内の対象日だけで計算します。

停止期間は対象日の一覧から除外します。停止前後に対象日が存在する場合でも、利用区間が分かれるため、集計の意味を確認できるよう有効期間数を内部結果へ保持します。

画面上は既存互換のため「日連続」と表示しますが、曜日指定習慣では対象日の連続回数を表します。

## 不整合データの扱い

達成記録が有効期間外または対象曜日外に存在する場合、日別画面では記録の存在を確認できることがありますが、達成率の分子・分母には含めません。

新しい単件・全件・選択一括達成では、有効期間外・停止期間・対象曜日外の登録をサーバー側で拒否します。既存の不整合記録は日別履歴の単件取り消し、指定日の全件一括取り消し、または選択一括取り消しで削除できます。

取り消し後にUndoを実行した場合は、操作前状態を正確に戻すため不整合記録も復元します。

JSONバックアップv5の検証CLIでは、以下をエラーとして検出します。

- 有効期間外の達成記録
- 曜日設定期間外の達成記録
- 対象曜日外の達成記録
- 曜日設定期間の重複・交差
- 開放中の曜日設定期間数の不整合

## 空データ

対象習慣がない日・週・月でも画面を表示します。

- 達成数: `0/0件`
- 達成率: `0%`
- 習慣別一覧: 空状態メッセージ

対象習慣も達成記録もない日には、一括操作セクションを表示しません。

CSVも200で返し、概要は0、習慣別集計はヘッダーだけを出力します。

0除算は行いません。

## 性能

現状は個人利用を前提とし、全習慣・有効期間・曜日設定期間を取得して、期間内の日数ごとに対象状態を計算します。

アクティブ習慣は最大20件ですが、終了済み習慣と各種期間は蓄積します。長期運用で件数が増えた場合は、次を検討します。

- 対象期間と交差する期間だけをSQLで取得
- 集計結果のキャッシュ
- 終了済み習慣のページング
- 年単位の集計テーブル
- 曜日マスクを利用したSQL側の事前絞り込み

## 移行上の制約

有効期間テーブル導入前は、複数回の終了・再開履歴を保存していません。既存データは次の1区間へ変換します。

- アクティブ習慣: 作成日〜継続中
- 終了済み習慣: 作成日〜直近終了日

曜日設定テーブル導入前は曜日変更履歴を保存していません。既存習慣は作成日から「毎日」として移行します。

過去の停止区間や曜日変更を推測して復元することはできません。移行後に日別履歴から達成状態を個別または一括修正する場合も、生成済みの有効期間・曜日設定期間を基準に判定します。

Undoは`app_settings`を利用するためDB移行は不要です。導入前の操作をUndoすることはできません。

## 未対応

- 複数日をまとめた一括更新
- 月次カレンダーからの直接編集
- 有効期間・曜日設定期間の手動編集
- 日別履歴CSV
- 曜日を固定しない週N回
- 隔週・月次スケジュール
- 年次集計
- 複数段階Undo・Redo
- 操作履歴・監査ログ
- タイムゾーン設定
