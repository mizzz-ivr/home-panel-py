# Home Panel 習慣トラッカー仕様

## 目的

毎日継続したい行動を単発ToDoとは分けて管理し、達成状況・過去実績・終了後の再利用まで扱います。

対象例:

- 30分読書
- ストレッチ
- 学習記録
- 日記を書く

## 基本操作

### 習慣を追加する

ダッシュボードの「習慣トラッカー」で習慣名を入力し、「追加」を押します。

入力制約:

- 1〜100文字
- 空白のみは不可
- アクティブな習慣と同じ名前は不可
- 大文字・小文字だけが異なる名前も同一扱い
- アクティブな習慣は最大20件

習慣本体と最初の有効期間は同じトランザクションで保存します。

### 今日の達成を記録する

各習慣の「達成」を押すと、当日の日付で達成記録を保存します。

同じ習慣・同じ日付の達成記録は1件だけです。DBの一意制約で重複を防ぎます。

### 今日の達成を取り消す

達成済みの習慣ではボタンが「取り消す」に変わります。押すと当日分の達成記録だけを削除します。

### 習慣を終了する

「終了」を押すと、習慣をアーカイブします。

- ダッシュボードには表示しない
- 習慣本体はDBに残す
- 過去の達成記録も残す
- `archived_at`へ終了日時を保存する
- 開放中の有効期間を終了日で閉じる
- 終了済み習慣への達成操作は拒否する

終了日当日は有効期間へ含めます。

### 習慣を管理する

習慣カードの「習慣を管理」から`/habits/manage`を開きます。

管理画面でできること:

- アクティブ習慣の名前編集
- 終了済み習慣の名前編集
- 終了日確認
- 終了済み習慣の再開
- アクティブ件数と上限確認

名前を変更しても習慣ID・達成履歴・有効期間履歴は変わりません。

### 終了済み習慣を再開する

再開時は以下を再検証します。

- アクティブ習慣が20件未満であること
- 同名のアクティブ習慣が存在しないこと
- 対象が終了済みであること

再開すると`is_active=true`、`archived_at=null`になり、新しい開放区間を追加します。過去の達成履歴と終了済み区間は保持します。

同じ日に終了・再開した場合は、日単位では停止期間が存在しないため、直前の区間を再オープンします。

## 有効期間の仕様

習慣が達成対象だった期間を`habit_active_periods`へ保存します。

例:

```text
7月1日〜3日: 有効
7月4日〜5日: 停止
7月6日〜: 有効
```

この場合、7月4日・5日は日別・週次・月次レポートの分母へ含めません。

- `started_on`: 有効期間の開始日
- `ended_on`: 有効期間の終了日。継続中はnull
- 開始日と終了日はどちらも対象日へ含める
- 同じ習慣・同じ開始日の区間は1件だけ
- 同じ習慣の区間は重複させない
- アクティブ習慣は開放中の区間を1件持つ
- 終了済み習慣は開放中の区間を持たない

## 連続日数の仕様

連続日数は保存せず、達成日から都度計算します。

- 今日が達成済み: 今日から過去へ連続する日数
- 今日が未達: 昨日から過去へ連続する日数
- 昨日も未達: 0日

今日の途中でまだ達成していないだけで、昨日までの連続記録を即座に0へしない仕様です。

停止期間を挟んだ場合、カレンダー上の日付が連続しないため連続日数は区間をまたいで継続しません。

## 履歴・集計

習慣カードから次の画面へ移動できます。

- 日別履歴: `/habits/history`
- 週次集計: `/habits/weekly`
- 月次集計: `/habits/monthly`

確認できる内容:

- 指定日の達成・未達成
- 期間全体の達成数と達成率
- 全対象習慣を達成した日数
- 日別達成率
- 習慣別達成日数
- 選択期間内の最長連続日数
- 終了済み習慣の過去実績

達成率の分母は、有効期間に含まれる習慣と日付の組み合わせです。作成前・終了後・停止中・未来日は対象外です。

詳細は`HABIT_REPORTS.md`を参照してください。

## データ設計

### `habits`

| 項目 | 内容 |
|---|---|
| `id` | 習慣ID |
| `name` | 習慣名 |
| `is_active` | 現在のアクティブ状態 |
| `archived_at` | 直近の終了日時。アクティブ時はnull |
| `created_at` | 作成日時 |
| `updated_at` | 最終更新日時 |

### `habit_active_periods`

| 項目 | 内容 |
|---|---|
| `id` | 有効期間ID |
| `habit_id` | 対象習慣ID |
| `started_on` | 開始日 |
| `ended_on` | 終了日。継続中はnull |
| `created_at` | 登録日時 |

`habit_id`と`started_on`の組み合わせには一意制約があります。`ended_on`はnullまたは`started_on`以降である必要があります。

### `habit_completions`

| 項目 | 内容 |
|---|---|
| `id` | 達成記録ID |
| `habit_id` | 対象習慣ID |
| `completed_on` | 達成日 |
| `created_at` | 登録日時 |

`habit_id`と`completed_on`の組み合わせには一意制約があります。

## 既存SQLiteの移行

アプリ起動時に習慣関連の互換移行を順番に実行します。

1. `habits.archived_at`がなければ追加
2. 終了済み習慣の`archived_at`を`updated_at`から補完
3. `habit_active_periods`がなければ作成
4. 既存習慣ごとに初期区間を1件生成

初期区間:

- アクティブ習慣: `created_at`の日付から継続中
- 終了済み習慣: `created_at`の日付から`archived_at`の日付まで

移行処理は複数回実行しても同じ結果になるよう設計しています。

移行前には複数回の終了・再開履歴が保存されていないため、過去の停止区間を推測して復元することはできません。移行時点では既存習慣を1区間として扱います。

現規模では軽量マイグレーションを採用していますが、今後DB変更が増える場合はAlembicなどの導入を検討します。

## カード設定との互換性

習慣カードのIDは`habits`です。

旧3カード設定が保存されている場合は、既存順序と非表示状態を維持し、習慣カードを末尾へ補完します。

## バックアップ

- v1: ToDo・日別メモ・時間記録
- v2: v1 + 習慣・習慣達成
- v3: v2 + `archived_at`
- v4: v3 + `habit_active_periods`

現在生成する形式はv4です。検証CLIはv1〜v4を受け付けます。

v4では区間重複、開放区間数、習慣状態、達成記録の期間内参照も検証します。

詳細は`BACKUP.md`を参照してください。

## エラー時の挙動

- 空白・長すぎる名前: 400
- 同名のアクティブ習慣: 400
- 21件目のアクティブ習慣: 400
- 存在しない習慣ID: 404
- アクティブ習慣への再開操作: 404
- 同名または上限到達時の再開: 400
- 履歴・週次の日付形式不正: 400
- 月次の月形式不正: 400
- 未来日・未来月: 400

エラー時も対象画面を表示し、理由を画面内へ表示します。

## 未対応

- 曜日指定・週N回などの頻度設定
- 過去日の達成編集
- 有効期間の手動編集
- 通知・リマインダー
- 習慣レポートCSV
- 年次集計
