# Home Panel 習慣トラッカー仕様

## 目的

毎日継続したい行動を、単発ToDoとは分けて管理します。

対象例:

- 30分読書
- ストレッチ
- 学習記録
- 日記を書く

## 基本操作

### 習慣を追加する

ダッシュボードの「習慣トラッカー」で習慣名を入力し、「追加」を押します。

入力制約:

- 1〜100文字
- 空白のみは不可
- アクティブな習慣と同じ名前は不可
- 大文字・小文字だけが異なる名前も同一扱い
- アクティブな習慣は最大20件

### 今日の達成を記録する

各習慣の「達成」を押すと、当日の日付で達成記録を保存します。

同じ習慣・同じ日付の達成記録は1件だけです。二重送信が発生してもDBの一意制約で重複を防ぎます。

### 今日の達成を取り消す

達成済みの習慣ではボタンが「取り消す」に変わります。押すと当日分の達成記録だけを削除します。

### 習慣を終了する

「終了」を押すと、習慣をアーカイブします。

- ダッシュボードには表示しない
- 習慣本体はDBに残す
- 過去の達成記録も残す
- 終了済み習慣への達成操作は拒否する

物理削除ではないため、将来の履歴表示や再開機能へ拡張できます。

## 連続日数の仕様

連続日数は保存せず、達成日から都度計算します。

- 今日が達成済み: 今日から過去へ連続する日数
- 今日が未達: 昨日から過去へ連続する日数
- 昨日も未達: 0日

今日の途中でまだ達成していないだけで、昨日までの連続記録を即座に0へしない仕様です。

例:

| 達成日 | 今日の状態 | 表示 |
|---|---|---:|
| 今日・昨日・一昨日 | 達成済み | 3日継続 |
| 昨日・一昨日 | 未達 | 2日継続 |
| 一昨日のみ | 未達 | 今日から開始 |

## データ設計

### `habits`

| 項目 | 内容 |
|---|---|
| `id` | 習慣ID |
| `name` | 習慣名 |
| `is_active` | アクティブ状態 |
| `created_at` | 作成日時 |
| `updated_at` | 更新日時 |

### `habit_completions`

| 項目 | 内容 |
|---|---|
| `id` | 達成記録ID |
| `habit_id` | 対象習慣ID |
| `completed_on` | 達成日 |
| `created_at` | 登録日時 |

`habit_id`と`completed_on`の組み合わせには一意制約があります。

## カード設定との互換性

習慣カードのIDは`habits`です。

旧3カード設定が保存されている場合は、既存順序と非表示状態を維持し、習慣カードを末尾へ補完します。

```json
{
  "order": ["time", "todo", "memo", "habits"],
  "hidden": ["memo"]
}
```

旧localStorage配置についても、既知カードの順序を維持し、不足カードを末尾へ補完してサーバー設定へ移行します。

## バックアップ

習慣データはJSONバックアップスキーマv2から対象です。

- `habits`
- `habit_completions`

過去のスキーマv1は習慣機能追加前の形式なので、ToDo・メモ・時間記録だけを含みます。検証CLIはv1とv2の両方を受け付けます。

詳細は`BACKUP.md`を参照してください。

## エラー時の挙動

- 空白・長すぎる名前: 400
- 同名のアクティブ習慣: 400
- 21件目のアクティブ習慣: 400
- 存在しない習慣ID: 404
- 終了済み習慣への操作: 404

エラー時もダッシュボードを表示し、理由を画面内へ表示します。

## 未対応

- 習慣名の編集
- 終了済み習慣の一覧・再開
- 曜日指定・週N回などの頻度設定
- 過去日の達成編集
- 習慣専用の日別・週次・月次集計
- 通知・リマインダー

これらを追加する場合も、`habits`と`habit_completions`の責務を維持し、既存データを破壊しない小さな差分で拡張します。
