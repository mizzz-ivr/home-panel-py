# Home Panel 習慣トラッカー仕様

## 目的

毎日継続したい行動を単発ToDoとは分けて管理し、達成状況・過去実績・終了後の再利用まで扱います。

対象例:

- 30分読書
- ストレッチ
- 学習記録
- 日記を書く

## 基本操作

### 習慣を追加する

ダッシュボードの「習慣トラッカー」で習慣名を入力し、「追加」を押します。

入力制約:

- 1〜100文字
- 空白のみは不可
- アクティブな習慣と同じ名前は不可
- 大文字・小文字だけが異なる名前も同一扱い
- アクティブな習慣は最大20件

### 今日の達成を記録する

各習慣の「達成」を押すと、当日の日付で達成記録を保存します。

同じ習慣・同じ日付の達成記録は1件だけです。DBの一意制約で重複を防ぎます。

### 今日の達成を取り消す

達成済みの習慣ではボタンが「取り消す」に変わります。押すと当日分の達成記録だけを削除します。

### 習慣を終了する

「終了」を押すと、習慣をアーカイブします。

- ダッシュボードには表示しない
- 習慣本体はDBに残す
- 過去の達成記録も残す
- `archived_at`へ終了日時を保存する
- 終了済み習慣への達成操作は拒否する

### 習慣を管理する

習慣カードの「習慣を管理」から`/habits/manage`を開きます。

管理画面でできること:

- アクティブ習慣の名前編集
- 終了済み習慣の名前編集
- 終了日確認
- 終了済み習慣の再開
- アクティブ件数と上限確認

名前を変更しても習慣IDと達成履歴は変わりません。

### 終了済み習慣を再開する

再開時は以下を再検証します。

- アクティブ習慣が20件未満であること
- 同名のアクティブ習慣が存在しないこと
- 対象が終了済みであること

再開すると`is_active=true`、`archived_at=null`になります。過去の達成履歴は保持します。

## 連続日数の仕様

連続日数は保存せず、達成日から都度計算します。

- 今日が達成済み: 今日から過去へ連続する日数
- 今日が未達: 昨日から過去へ連続する日数
- 昨日も未達: 0日

今日の途中でまだ達成していないだけで、昨日までの連続記録を即座に0へしない仕様です。

## 履歴・集計

習慣カードから次の画面へ移動できます。

- 日別履歴: `/habits/history`
- 週次集計: `/habits/weekly`
- 月次集計: `/habits/monthly`

確認できる内容:

- 指定日の達成・未達成
- 期間全体の達成数と達成率
- 全対象習慣を達成した日数
- 日別達成率
- 習慣別達成日数
- 選択期間内の最長連続日数
- 終了済み習慣の過去実績

終了済み習慣の有効期間は`created_at`から`archived_at`までです。終了後に名前を編集して`updated_at`が変わっても、レポート上の終了日は変わりません。

詳細は`HABIT_REPORTS.md`を参照してください。

## データ設計

### `habits`

| 項目 | 内容 |
|---|---|
| `id` | 習慣ID |
| `name` | 習慣名 |
| `is_active` | アクティブ状態 |
| `archived_at` | 終了日時。アクティブ時はnull |
| `created_at` | 作成日時 |
| `updated_at` | 最終更新日時 |

### `habit_completions`

| 項目 | 内容 |
|---|---|
| `id` | 達成記録ID |
| `habit_id` | 対象習慣ID |
| `completed_on` | 達成日 |
| `created_at` | 登録日時 |

`habit_id`と`completed_on`の組み合わせには一意制約があります。

## 既存SQLiteの移行

アプリ起動時に`habits.archived_at`の有無を確認します。

既存DBに列がない場合:

1. `ALTER TABLE habits ADD COLUMN archived_at DATETIME`を実行
2. 終了済み習慣は`updated_at`を初期終了日時として補完
3. アクティブ習慣の`archived_at`はnullへ補正

移行処理は複数回実行しても同じ結果になるよう設計しています。

現規模では軽量マイグレーションを採用していますが、今後DB変更が増える場合はAlembicなどの導入を検討します。

## 再開後の集計に関する制約

現行モデルは「終了期間の履歴」を別テーブルで持っていません。

一度終了した習慣を再開した場合、レポートでは作成日から現在までを1つの有効期間として扱います。終了中の空白期間を厳密に分母から除外する機能は未対応です。

この要件を追加する場合は、状態変更履歴テーブルを導入して有効期間を複数区間として管理する必要があります。

## カード設定との互換性

習慣カードのIDは`habits`です。

旧3カード設定が保存されている場合は、既存順序と非表示状態を維持し、習慣カードを末尾へ補完します。

## バックアップ

- v1: ToDo・日別メモ・時間記録
- v2: v1 + 習慣・習慣達成
- v3: v2 + `archived_at`

現在生成する形式はv3です。検証CLIはv1・v2・v3を受け付けます。

詳細は`BACKUP.md`を参照してください。

## エラー時の挙動

- 空白・長すぎる名前: 400
- 同名のアクティブ習慣: 400
- 21件目のアクティブ習慣: 400
- 存在しない習慣ID: 404
- アクティブ習慣への再開操作: 404
- 同名または上限到達時の再開: 400
- 履歴・週次の日付形式不正: 400
- 月次の月形式不正: 400
- 未来日・未来月: 400

エラー時も対象画面を表示し、理由を画面内へ表示します。

## 未対応

- 曜日指定・週N回などの頻度設定
- 過去日の達成編集
- 通知・リマインダー
- 習慣レポートCSV
- 年次集計
- 終了・再開を複数区間として扱う状態変更履歴
