# Home Panel 習慣トラッカー仕様

## 目的

継続したい行動を単発ToDoとは分けて管理し、対象曜日・達成状況・過去実績・終了後の再利用まで扱います。

対象例:

- 毎日30分読書
- 平日の学習
- 月・水・金の運動
- 土日の振り返り

## 基本操作

### 習慣を追加する

ダッシュボードの「習慣トラッカー」で習慣名を入力し、「追加」を押します。

入力制約:

- 1〜100文字
- 空白のみは不可
- アクティブな習慣と同じ名前は不可
- 大文字・小文字だけが異なる名前も同一扱い
- アクティブな習慣は最大20件

新規習慣は初期状態では「毎日」です。習慣本体、最初の有効期間、最初の曜日設定期間を同じトランザクションで保存します。

### 対象曜日を設定する

`/habits/manage`で月曜日〜日曜日を1つ以上選択します。

表示例:

- 全曜日: `毎日`
- 月曜日〜金曜日: `平日`
- 土曜日・日曜日: `土日`
- 月曜日・水曜日・金曜日: `月・水・金`

曜日設定は保存日から適用し、変更前の設定を履歴として保持します。詳細は`HABIT_SCHEDULES.md`を参照してください。

### 今日の達成を記録する

今日が対象曜日の場合だけ「達成」ボタンを表示します。押すと当日の日付で達成記録を保存します。

同じ習慣・同じ日付の達成記録は1件だけです。DBの一意制約で重複を防ぎます。

対象外曜日は次の扱いです。

- ダッシュボードには習慣を表示する
- `今日は対象外`と表示する
- 達成ボタンを無効化する
- 今日の達成件数の分母へ含めない
- 直接POSTされても新しい達成記録を保存しない

### 今日の達成を取り消す

達成済みの習慣ではボタンが「取り消す」に変わります。押すと当日分の達成記録だけを削除します。

既に存在する対象外曜日の不整合記録についても、取り消し操作は許可します。

### 習慣を終了する

「終了」を押すと、習慣をアーカイブします。

- ダッシュボードには表示しない
- 習慣本体はDBに残す
- 過去の達成記録も残す
- `archived_at`へ終了日時を保存する
- 開放中の有効期間を終了日で閉じる
- 曜日設定履歴は保持する
- 終了済み習慣への達成操作は拒否する

終了日当日は有効期間へ含めます。

### 習慣を管理する

習慣カードの「習慣を管理」から`/habits/manage`を開きます。

管理画面でできること:

- アクティブ習慣の名前編集
- 終了済み習慣の名前編集
- 対象曜日の確認・変更
- 終了日確認
- 終了済み習慣の再開
- アクティブ件数と上限確認

名前を変更しても習慣ID・達成履歴・有効期間履歴・曜日設定履歴は変わりません。

終了済み習慣も曜日設定を変更できます。終了中に保存した曜日は、再開後の対象判定に利用します。

### 終了済み習慣を再開する

再開時は以下を再検証します。

- アクティブ習慣が20件未満であること
- 同名のアクティブ習慣が存在しないこと
- 対象が終了済みであること

再開すると`is_active=true`、`archived_at=null`になり、新しい開放区間を追加します。過去の達成履歴・終了済み区間・曜日設定履歴は保持します。

同じ日に終了・再開した場合は、日単位では停止期間が存在しないため、直前の有効期間を再オープンします。

## 有効期間の仕様

習慣が利用中だった期間を`habit_active_periods`へ保存します。

```text
7月1日〜3日: 有効
7月4日〜5日: 停止
7月6日〜: 有効
```

7月4日・5日は日別・週次・月次レポートの分母へ含めません。

- `started_on`: 有効期間の開始日
- `ended_on`: 有効期間の終了日。継続中はnull
- 開始日と終了日はどちらも対象日へ含める
- 同じ習慣・同じ開始日の区間は1件だけ
- 同じ習慣の区間は重複させない
- アクティブ習慣は開放中の区間を1件持つ
- 終了済み習慣は開放中の区間を持たない

## 曜日設定期間の仕様

曜日設定を`habit_schedule_periods`へ保存します。

```text
7月1日〜9日: 毎日
7月10日〜: 月・水・金
```

7月9日以前のレポートは毎日設定のままです。7月10日以降だけ月・水・金として集計します。

- `schedule_type`: 現在は`weekdays`固定
- `weekdays_mask`: 対象曜日の7ビット値
- `started_on`: 設定の適用開始日
- `ended_on`: 適用終了日。現在設定はnull
- 曜日は最低1つ必要
- 各習慣は開放中の曜日設定を1件持つ
- 同一習慣の曜日設定期間は重複させない

## 連続日数の仕様

連続値は保存せず、対象日と達成記録から都度計算します。

毎日の習慣では従来どおり連続日数です。曜日指定習慣では、カレンダー日ではなく対象日の連続達成回数として計算します。

例:

```text
対象: 月・水・金
月: 達成
水: 達成
金: 達成
```

画面には既存互換のため`3日継続`と表示します。火・木は対象外なので連続を切りません。

今日が対象日でまだ未達成の場合は、今日の途中であることを考慮し、直前の対象日までの連続を表示します。

## 履歴・集計

習慣カードから次の画面へ移動できます。

- 日別履歴: `/habits/history`
- 週次集計: `/habits/weekly`
- 月次集計: `/habits/monthly`

確認できる内容:

- 指定日の達成・未達成・対象外
- 期間全体の達成数と達成率
- 全対象習慣を達成した日数
- 日別達成率
- 習慣別達成日数
- 選択期間内の最長連続値
- 終了済み習慣の過去実績

達成率の分母は、以下の両方を満たす習慣と日付の組み合わせです。

1. 有効期間内
2. その時点の曜日設定で対象曜日

作成前・終了後・停止中・対象曜日外・未来日は対象外です。詳細は`HABIT_REPORTS.md`を参照してください。

## データ設計

### `habits`

| 項目 | 内容 |
|---|---|
| `id` | 習慣ID |
| `name` | 習慣名 |
| `is_active` | 現在のアクティブ状態 |
| `archived_at` | 直近の終了日時。アクティブ時はnull |
| `created_at` | 作成日時 |
| `updated_at` | 最終更新日時 |

### `habit_active_periods`

| 項目 | 内容 |
|---|---|
| `id` | 有効期間ID |
| `habit_id` | 対象習慣ID |
| `started_on` | 開始日 |
| `ended_on` | 終了日。継続中はnull |
| `created_at` | 登録日時 |

### `habit_schedule_periods`

| 項目 | 内容 |
|---|---|
| `id` | 曜日設定期間ID |
| `habit_id` | 対象習慣ID |
| `schedule_type` | `weekdays` |
| `weekdays_mask` | 対象曜日のビットマスク |
| `started_on` | 適用開始日 |
| `ended_on` | 適用終了日。現在設定はnull |
| `created_at` | 登録日時 |

### `habit_completions`

| 項目 | 内容 |
|---|---|
| `id` | 達成記録ID |
| `habit_id` | 対象習慣ID |
| `completed_on` | 達成日 |
| `created_at` | 登録日時 |

`habit_id`と`completed_on`の組み合わせには一意制約があります。

## 既存SQLiteの移行

アプリ起動時に習慣関連の互換移行を順番に実行します。

1. `habits.archived_at`がなければ追加
2. 終了済み習慣の`archived_at`を`updated_at`から補完
3. `habit_active_periods`がなければ作成
4. 既存習慣ごとに初期有効期間を生成
5. `habit_schedule_periods`がなければ作成
6. 既存習慣ごとに作成日からの毎日設定を生成

移行処理は複数回実行しても同じ結果になる冪等設計です。

移行前に保存されていない過去の停止区間や曜日変更履歴は推測できません。既存習慣は1つの有効期間と1つの毎日設定として移行します。

現規模では軽量マイグレーションを採用していますが、DB変更が増える場合はAlembicなどの導入を検討します。

## バックアップ

- v1: ToDo・日別メモ・時間記録
- v2: v1 + 習慣・習慣達成
- v3: v2 + `archived_at`
- v4: v3 + `habit_active_periods`
- v5: v4 + `habit_schedule_periods`

現在生成する形式はv5です。検証CLIはv1〜v5を受け付けます。

詳細は`BACKUP.md`を参照してください。

## エラー時の挙動

- 空白・長すぎる名前: 400
- 同名のアクティブ習慣: 400
- 21件目のアクティブ習慣: 400
- 存在しない習慣ID: 404
- アクティブ習慣への再開操作: 404
- 同名または上限到達時の再開: 400
- 曜日未選択・範囲外: 400
- 変更日の達成記録と曜日設定が矛盾: 400
- 履歴・週次の日付形式不正: 400
- 月次の月形式不正: 400
- 未来日・未来月: 400

エラー時も対象画面を表示し、理由を画面内へ表示します。

## 未対応

- 曜日を固定しない週N回
- 隔週・月次スケジュール
- 過去日の達成編集
- 有効期間・曜日設定期間の手動編集
- 通知・リマインダー
- 習慣レポートCSV
- 年次集計
- タイムゾーン設定
