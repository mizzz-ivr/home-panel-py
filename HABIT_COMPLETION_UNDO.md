# Home Panel 習慣達成Undo仕様

## 目的

習慣の達成・取り消しを誤操作した場合に、直前の状態へ安全に戻せるようにします。

特に、指定日の全達成取り消しや選択一括操作は影響範囲が大きいため、確認ダイアログに加えて操作後の回復手段を提供します。

## 対象操作

次の達成状態変更でUndoを記録します。

| 操作 | 操作種別 |
|---|---|
| ダッシュボードの当日トグル | `dashboard_toggle` |
| 日別履歴の単件達成 | `single_complete` |
| 日別履歴の単件取り消し | `single_clear` |
| 対象習慣の全件一括達成 | `bulk_complete` |
| 指定日の全達成取り消し | `bulk_clear` |
| 選択した習慣の一括達成 | `selected_complete` |
| 選択した達成の一括取り消し | `selected_clear` |

習慣名変更、習慣作成、ToDo・メモ・時間記録は対象外です。

## 基本仕様

- Undoは直前の1操作だけ保存
- 有効期限は操作から10分
- 新しい達成操作で古いUndoを置き換える
- アプリ再起動後も、期限内かつ状態が一致する場合は利用可能
- Undoを1回実行すると保存情報を削除
- Redoと複数段階Undoは未対応

## 保存先

既存の`app_settings`テーブルを利用します。

```text
key = habit.completion.undo.v1
```

新しいテーブル・列・マイグレーションは追加しません。

保存例:

```json
{
  "version": 1,
  "token": "ランダムな操作トークン",
  "target_date": "2026-07-31",
  "before_habit_ids": [1, 3],
  "after_habit_ids": [1, 2, 3],
  "source": "selected_complete",
  "created_at": "2026-07-31T05:30:00+00:00",
  "expires_at": "2026-07-31T05:40:00+00:00"
}
```

### 保存値の検証

読み込み時に次を検証します。

- `version = 1`
- トークンが16〜128文字
- 対象日がISO日付
- 習慣IDが正の整数
- 習慣IDに重複がない
- 操作前・操作後集合が異なる
- 操作種別が既知の値
- 作成日時・期限がタイムゾーン付き日時
- 期限が作成日時より後
- 有効期間が10分以内

壊れたJSONや不正な保存値は、利用可能なUndoとして扱いません。

## 操作前・操作後集合

Undoでは、指定日の達成記録を習慣ID集合として保存します。

```text
操作前: [1, 3]
操作後: [1, 2, 3]
```

復元時は、指定日の状態を操作前集合へ戻します。

個別の追加・削除差分だけでなく日全体の集合を保存することで、全件取り消しや選択取り消しも同じ処理で復元できます。

## 復元条件

Undo実行前に次を確認します。

1. 保存されたUndoが存在する
2. 送信トークンが保存トークンと一致する
3. 有効期限内である
4. 現在の対象日達成集合が保存した操作後集合と完全一致する
5. 操作前集合が参照する習慣がすべて存在する

### 後続変更の保護

現在状態が操作後集合と異なる場合、復元しません。

```text
操作後集合: [1, 2]
現在集合:   [1, 2, 3]
```

この場合は409を返し、現在集合を維持します。

別タブ操作、手動DB更新、後続の達成変更を古いUndoで上書きしないための条件です。

### 競合再試行

一括達成・選択一括達成で一意制約競合が発生した場合は、ロールバック後の最新集合を新しい操作前状態として扱います。

他処理が同時に追加した記録をUndoで削除しないようにします。

## 復元処理

復元は次を同一トランザクションで行います。

1. 対象日の現在の達成記録を削除
2. 操作前集合を再作成
3. Undo設定を削除
4. コミット

復元途中で失敗した場合、達成記録とUndo設定の両方を確定しません。

## 不整合記録の扱い

一括取り消し前に対象曜日外・有効期間外の達成記録が存在していた場合も、操作前集合として保存します。

Undoは操作前状態への回復機能であり、整合性の自動修正機能ではありません。そのため、不整合記録も正確に復元します。

不整合を削除したい場合は、Undoを使わず取り消し後の状態を維持します。

## Undoの失効

次の操作は達成対象の前提を変えるため、保存中のUndoを削除します。

- 曜日設定変更
- 習慣終了
- 習慣再開

達成状態を変更しない習慣名変更では失効しません。

## トランザクション方針

### 達成記録本体

単件・全件・選択一括の達成記録更新は、従来どおり1回のコミットで確定します。

### Undoメタデータ

達成記録本体の確定後、短命な別SQLAlchemy Sessionで`app_settings`へ保存します。

理由:

- 達成記録本体の既存1コミット契約を維持する
- Undo保存障害で達成操作自体を失敗させない
- Undoを補助機能として扱う

Undo保存に失敗した場合:

- 達成記録の変更は維持
- Undo通知は表示されない
- 通常操作はエラーにしない

Undo復元自体は、達成記録とUndo消費を同じコミットで行います。

## API

### 状態取得

```text
GET /habits/completions/undo?target_date=YYYY-MM-DD
```

利用可能な場合:

```json
{
  "available": true,
  "token": "...",
  "target_date": "2026-07-31",
  "label": "選択した習慣の一括達成",
  "expires_in_seconds": 540
}
```

利用できない場合:

```json
{
  "available": false
}
```

状態取得では次も確認します。

- 対象日が一致する
- 期限内
- 現在状態が操作後集合と一致する

### 復元

```text
POST /habits/completions/undo

token=<Undoトークン>
```

成功時は303でリダイレクトします。

| 操作元 | 戻り先 |
|---|---|
| ダッシュボード | `/` |
| 日別履歴 | `/habits/history?target_date=YYYY-MM-DD` |

戻り先は保存された操作種別からサーバー側で決定します。フォームから任意URLは受け付けません。

### エラーステータス

| 条件 | ステータス |
|---|---:|
| Undoなし | 404 |
| 不正・不一致トークン | 400 |
| 期限切れ | 410 |
| 後続変更あり | 409 |
| 復元対象習慣が存在しない | 409 |

### HTTPヘッダー

状態取得・復元応答に次を設定します。

```text
Cache-Control: no-store
X-Content-Type-Options: nosniff
```

## UI

ダッシュボードと習慣日別履歴にUndo通知領域を置きます。

JavaScriptは次を行います。

1. ページの対象日を取得
2. 状態APIへ問い合わせ
3. 利用可能な場合だけ通知とボタンを生成
4. 有効期限後に通知を非表示

DOM生成には`createElement`と`textContent`を使用し、`innerHTML`は使用しません。

状態APIの失敗やJavaScript無効時も、通常の習慣操作は利用できます。ただし、Undo通知の表示にはJavaScriptが必要です。

## バックアップ境界

Pending Undoは一時的な操作補助情報として扱い、JSONバックアップv5へ含めません。

バックアップ対象:

- 習慣本体
- 達成記録
- 有効期間
- 曜日設定期間

バックアップ対象外:

- `app_settings`内のPending Undo

復元後に古い一時トークンが残ることを避けるためです。

## セキュリティ・運用前提

- ローカル個人利用を前提
- UndoトークンはSQLiteへ平文保存
- 状態APIは同一オリジンUIへトークンを返す
- 認証・ユーザー分離は未対応
- CSRF対策は未対応
- グローバルに直前1操作のみ

外部公開する場合は、認証・ユーザー単位保存・CSRF対策・セッション管理が必要です。

## テスト観点

- 保存値の構造・厳密検証
- 直前操作への置き換え
- 無変更操作で既存Undoを維持
- 単件達成・取り消し
- ダッシュボードトグル
- 全件一括操作
- 選択一括操作
- 未選択記録の維持
- 不整合記録の復元
- 一度限りの消費
- トークン不一致
- 期限切れ
- 後続変更
- 習慣欠落
- 曜日変更・終了・再開による失効
- 競合再試行時の操作前集合
- Undo保存失敗時の本体維持
- APIヘッダー
- 安全な戻り先
- JavaScript・CSS配信
- ダッシュボード・日別履歴の通知領域
- 既存達成処理の1コミット保証

## 未対応

- 複数段階Undo
- Redo
- 操作履歴・監査ログ
- ユーザーごとのUndo
- JavaScriptなしの通知表示
- 認証・ユーザー分離
- CSRF対策
- タイムゾーン設定
