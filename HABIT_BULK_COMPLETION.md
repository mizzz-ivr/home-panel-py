# Home Panel 習慣達成一括操作仕様

## 目的

習慣の日別履歴で、指定日の達成対象をまとめて達成済みにしたり、全件または選択した項目の達成記録を取り消したりできるようにします。

単件更新を繰り返すのではなく、1回の操作を1トランザクションで処理し、途中失敗による部分更新を防ぎます。

## エンドポイント

### 全件一括操作

```text
POST /habits/completions/bulk
```

フォーム項目:

```text
target_date=YYYY-MM-DD
action=complete_expected|clear_all
```

| action | 処理 |
|---|---|
| `complete_expected` | 指定日に達成対象だった未達成習慣をすべて達成済みにする |
| `clear_all` | 指定日の達成記録をすべて削除する |

### 選択一括操作

```text
POST /habits/completions/selected
```

フォーム項目:

```text
target_date=YYYY-MM-DD
completed=true|false
habit_ids=<習慣ID>（複数指定）
```

| completed | 処理 |
|---|---|
| `true` | 選択した対象習慣のうち未達成のものを達成済みにする |
| `false` | 選択した習慣の既存達成記録だけを削除する |

成功時は次のURLへ303リダイレクトします。

```text
/habits/history?target_date=YYYY-MM-DD
```

リダイレクト先はサーバー側で生成し、フォームから任意URLは受け付けません。

## 全件一括達成

### 追加対象

指定日に次の条件をすべて満たす未達成習慣だけを追加します。

```text
対象日 <= 今日
AND 習慣の有効期間内
AND 曜日設定期間内
AND 対象曜日
AND 達成記録なし
```

対象判定は日別レポートと共通の処理を利用します。通常は`habit_active_periods`と`habit_schedule_periods`を正とし、移行途中で履歴がない旧データでは既存項目による互換判定を利用します。

### 追加しないケース

- 未来日
- 習慣作成前
- 習慣終了後
- 終了から再開までの停止期間
- 対象曜日外
- 既に達成済み

対象外の日に既存の不整合記録がある場合、全件一括達成では削除・変更しません。意図しないデータ削除を避けるためです。

## 全件一括取り消し

指定日の`habit_completions`をすべて削除します。

削除対象:

- 正常な達成記録
- 対象曜日外の不整合記録
- 有効期間外の不整合記録

バックアップ検証CLIで検出された不整合記録をまとめて修復する用途にも利用できます。

## 選択一括達成

利用者が日別履歴でチェックした習慣だけを達成済みにします。

### 検証条件

更新前に、選択されたすべてのIDについて次を確認します。

```text
選択件数 >= 1
AND IDは正の整数
AND ID重複なし
AND すべての習慣が存在
AND すべて指定日の達成対象
AND 対象日 <= 今日
```

1件でも未知ID・重複ID・対象外習慣が含まれる場合、正常な選択項目も含めて更新しません。

### 更新内容

- 選択済みかつ未達成の対象習慣だけを追加
- 選択済みかつ達成済みの項目は変更なし
- 選択していない習慣は変更なし
- 1回のコミットで複数件を確定

対象曜日外・停止期間・作成前・終了後の習慣を混在させた場合は、全選択項目を拒否します。

## 選択一括取り消し

利用者がチェックした習慣のうち、指定日に達成記録が存在するものだけを削除します。

- 未達成の選択項目は変更なし
- 選択していない達成記録は維持
- 対象曜日外・有効期間外の不整合記録も削除可能
- 1回のコミットで複数件を確定

取り消しでは対象曜日や有効期間を条件にしません。既存の不整合記録を修復できるようにするためです。ただし、選択IDの形式・重複・存在と未来日は検証します。

## トランザクション

### 達成追加

1. 全入力を検証
2. 指定日の対象習慣IDを取得
3. 既存の達成記録IDを取得
4. 未達成の対象習慣だけをセッションへ追加
5. 1回だけコミット

同時実行で一意制約競合が発生した場合:

1. ロールバック
2. 現在の達成記録を再取得
3. 不足分を再計算
4. 不足分だけを再追加してコミット

競合後にすべて達成済みであれば、正常な再送として変更なしを返します。

### 達成取り消し

1. 全入力を検証
2. 指定日の達成記録を取得
3. 全件操作または選択IDに応じて削除対象を決定
4. 対象をセッションから削除
5. 1回だけコミット

単件更新処理をループして複数回コミットする構成にはしていません。

## 冪等性

同じ操作を複数回送信しても、最終状態は変わりません。

```text
全件・選択一括達成を2回送信
-> 対象習慣ごとの達成記録は1件
```

```text
全件・選択一括取り消しを2回送信
-> 削除対象の達成記録は0件
```

## 画面表示

### 全件一括操作

日別履歴で操作可能な場合だけ「この日の一括操作」を表示します。

| 状態 | 表示する操作 |
|---|---|
| 未達成の対象習慣がある | `対象習慣をすべて達成` |
| 達成記録が1件以上ある | `この日の達成をすべて取り消す` |
| 対象習慣も達成記録もない | 一括操作セクションを非表示 |
| 全対象習慣が達成済み | 一括達成を非表示 |

### 選択一括操作

対象習慣または達成記録がある場合に「習慣を選んで操作」を表示します。

- 達成対象または達成記録がある項目にチェックボックスを表示
- 対象曜日外で記録がない項目にはチェックボックスを表示しない
- チェックボックスは`form`属性で選択操作フォームへ関連付け
- 既存の単件操作フォームと入れ子にしない
- 選択なしで送信した場合は400

確認ダイアログは既存の`data-confirm`処理を利用します。

JavaScriptが無効でもPOST自体は利用できます。確認ダイアログは補助であり、安全性はサーバー側の入力・対象判定に依存します。

## 入力検証

### 共通

- 日付は厳密な`YYYY-MM-DD`
- 実在しない日付は拒否
- 未来日は拒否
- 不正入力は400

### 全件一括操作

- 操作種別は`complete_expected`または`clear_all`だけ

### 選択一括操作

- 達成状態は`true`または`false`だけ
- 習慣IDを1件以上指定
- 習慣IDは正の整数
- 習慣IDの重複を拒否
- 未知IDを含む場合は404
- 達成追加時に対象外習慣を含む場合は400

## データ更新範囲

更新対象は`habit_completions`だけです。

変更しないもの:

- `habits`
- `habit_active_periods`
- `habit_schedule_periods`
- DBスキーマ
- 習慣名・現在状態
- 曜日設定
- ToDo・メモ・時間記録

## テスト観点

- 対象習慣だけを全件一括達成
- 選択した対象習慣だけを達成
- 未選択習慣を変更しない
- 対象曜日外・停止期間を追加しない
- 対象外習慣混在時に部分更新しない
- 未知ID混在時に部分更新しない
- 空・重複・不正IDを拒否
- 終了済み習慣の有効期間内は追加
- 既存達成記録を重複追加しない
- 既存不整合記録を達成操作で変更しない
- 正常記録と不整合記録の全件取り消し
- 選択した記録だけの取り消し
- 全件・選択操作の再送
- 未来日・不正日付・不正操作の拒否
- 成功時の303リダイレクト
- ボタン・チェックボックスの表示条件
- 外部フォーム関連付け
- 追加・削除のコミット回数
- 単件編集と既存集計の回帰

## 未対応

- 複数日をまとめた一括更新
- 月次カレンダーからの直接操作
- 操作履歴・監査ログ
- 取り消し後のUndo
- 認証・ユーザー分離
- CSRF対策
- タイムゾーン設定
