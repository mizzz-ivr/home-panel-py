# Home Panel 習慣達成一括操作仕様

## 目的

習慣の日別履歴で、指定日の達成対象をまとめて達成済みにしたり、その日の達成記録をまとめて取り消したりできるようにします。

単件更新を繰り返すのではなく、1回の操作を1トランザクションで処理し、途中失敗による部分更新を防ぎます。

## エンドポイント

```text
POST /habits/completions/bulk
```

フォーム項目:

```text
target_date=YYYY-MM-DD
action=complete_expected|clear_all
```

| action | 処理 |
|---|---|
| `complete_expected` | 指定日に達成対象だった未達成習慣をすべて達成済みにする |
| `clear_all` | 指定日の達成記録をすべて削除する |

成功時は次のURLへ303リダイレクトします。

```text
/habits/history?target_date=YYYY-MM-DD
```

リダイレクト先はサーバー側で生成し、フォームから任意URLは受け付けません。

## 一括達成

### 追加対象

指定日に次の条件をすべて満たす未達成習慣だけを追加します。

```text
対象日 <= 今日
AND 習慣の有効期間内
AND 曜日設定期間内
AND 対象曜日
AND 達成記録なし
```

対象判定は日別レポートと共通の処理を利用します。通常は`habit_active_periods`と`habit_schedule_periods`を正とし、移行途中で履歴がない旧データでは既存項目による互換判定を利用します。

### 追加しないケース

- 未来日
- 習慣作成前
- 習慣終了後
- 終了から再開までの停止期間
- 対象曜日外
- 既に達成済み

対象外の日に既存の不整合記録がある場合、一括達成では削除・変更しません。意図しないデータ削除を避けるためです。

## 一括取り消し

指定日の`habit_completions`をすべて削除します。

削除対象:

- 正常な達成記録
- 対象曜日外の不整合記録
- 有効期間外の不整合記録

バックアップ検証CLIで検出された不整合記録をまとめて修復する用途にも利用できます。

## トランザクション

### 一括達成

1. 指定日の対象習慣IDを取得
2. 既存の達成記録IDを取得
3. 未達成の対象習慣だけをセッションへ追加
4. 1回だけコミット

同時実行で一意制約競合が発生した場合:

1. ロールバック
2. 現在の達成記録を再取得
3. 不足分を再計算
4. 不足分だけを再追加してコミット

競合後にすべて達成済みであれば、正常な再送として変更なしを返します。

### 一括取り消し

1. 指定日の全達成記録を取得
2. 全件をセッションから削除
3. 1回だけコミット

単件更新処理をループして複数回コミットする構成にはしていません。

## 冪等性

同じ操作を複数回送信しても、最終状態は変わりません。

```text
一括達成を2回送信
-> 対象習慣ごとの達成記録は1件
```

```text
一括取り消しを2回送信
-> 指定日の達成記録は0件
```

## 画面表示

日別履歴で操作可能な場合だけ「この日の一括操作」を表示します。

| 状態 | 表示する操作 |
|---|---|
| 未達成の対象習慣がある | `対象習慣をすべて達成` |
| 達成記録が1件以上ある | `この日の達成をすべて取り消す` |
| 対象習慣も達成記録もない | 一括操作セクションを非表示 |
| 全対象習慣が達成済み | 一括達成を非表示 |

一括取り消しフォームには`data-confirm`を設定し、共通JavaScriptで確認ダイアログを表示します。

JavaScriptが無効でもPOST自体は利用できます。確認ダイアログは補助であり、安全性はサーバー側の日付・操作種別検証に依存します。

## 入力検証

- 日付は厳密な`YYYY-MM-DD`
- 実在しない日付は拒否
- 未来日は拒否
- 操作種別は`complete_expected`または`clear_all`だけ
- 不正入力は400

## データ更新範囲

更新対象は`habit_completions`だけです。

変更しないもの:

- `habits`
- `habit_active_periods`
- `habit_schedule_periods`
- DBスキーマ
- 習慣名・現在状態
- 曜日設定
- ToDo・メモ・時間記録

## テスト観点

- 対象習慣だけを一括達成
- 対象曜日外を追加しない
- 停止期間を追加しない
- 終了済み習慣の有効期間内は追加
- 既存達成記録を重複追加しない
- 既存不整合記録を一括達成で変更しない
- 正常記録と不整合記録の一括取り消し
- 一括達成・一括取り消しの再送
- 未来日・不正日付・不正操作の拒否
- 成功時の303リダイレクト
- ボタン表示条件
- 確認属性
- 一括達成のコミット回数
- 単件編集と既存集計の回帰

## 未対応

- 習慣を選択して一部だけ一括更新
- 複数日をまとめた一括更新
- 操作履歴・監査ログ
- 取り消し後のUndo
- 認証・ユーザー分離
- CSRF対策
- タイムゾーン設定
