# ToDoクイックビュー仕様

## 目的

期限・優先度を設定したToDoから、今確認したいタスクへすぐ絞り込めるようにします。

DBへフィルタ設定を保存せず、ダッシュボードURLのクエリだけで表示状態を表します。

## URL

```text
/?todo_view=<view>#todo-card
```

許容値:

| 値 | 表示 | 対象 |
|---|---|---|
| `all` | すべて | 全ToDo |
| `today` | 今日 | 未完了かつ期限が今日 |
| `overdue` | 期限切れ | 未完了かつ期限が今日より前 |
| `upcoming` | 今後 | 未完了かつ期限が今日より後 |
| `no_due` | 期限なし | 未完了かつ期限未設定 |
| `high` | 高優先度 | 未完了かつ優先度が高 |
| `completed` | 完了済み | 完了済みToDo |

不明な`todo_view`はエラーにせず`all`として表示します。

## 件数

各フィルタには、全ToDoを母集団として現在の件数を表示します。

- `high`は完了済みの高優先度ToDoを含めない
- 期限系フィルタも完了済みToDoを含めない
- `all`は完了済みを含む全件数
- `completed`は完了済みだけの件数

選択中の条件について、`全件数中 表示件数`も表示します。

## 表示順

フィルタは既存のToDo並び順を変更しません。

表示対象を絞るだけで、対象内の順番は従来どおりです。

1. 期限状態
2. 優先度
3. 期限日
4. 作成日時
5. ID

## 操作後の表示状態

クイックビュー中に次の操作を行っても、同じ`todo_view`へ戻ります。

- ToDo追加
- 期限・優先度編集
- 完了・未完了切替
- 削除

操作結果によって現在の条件から外れたToDoは、戻った画面では表示されません。

例:

- `high`で高優先度ToDoを完了すると、`high`一覧から消える
- `completed`で未完了へ戻すと、`completed`一覧から消える
- `overdue`で期限を将来日に変えると、`overdue`一覧から消える

## 非表示ToDoカード

横断検索から`show_card=todo`で一時表示されたToDoカードでは、クイックビューリンクとToDo操作後の戻り先にも`show_card=todo`を維持します。

`show_card`は`todo`だけを許可し、任意URLや任意文字列を戻り先として利用しません。

## 空状態

- `all`が0件: `タスクはまだありません。`
- その他のビューが0件: `この条件に一致するタスクはありません。`

## データへの影響

クイックビューの選択自体は読み取り専用です。

変更しないもの:

- DBスキーマ
- JSONバックアップv6
- ToDoの保存データ
- ダッシュボード表示設定
- メモ・時間記録・習慣

## テスト観点

- 各期限ビューで対象だけ表示
- 高優先度で完了済みを除外
- 完了済みビュー
- 不正ビューの`all`フォールバック
- フィルタ表示前後でToDo件数不変
- 件数表示
- `show_card=todo`維持
- 作成後のビュー維持
- 完了切替後のビュー維持
- 削除後のビュー維持
- 任意文字列を戻り先へ利用しないこと
- 既存ToDo・他カードの回帰

## 対象外

- フィルタ状態のDB保存
- 複数条件のAND/OR指定
- ユーザー定義フィルタ
- タグ・プロジェクト分類
- 通知・リマインダー
- 繰り返しToDo
